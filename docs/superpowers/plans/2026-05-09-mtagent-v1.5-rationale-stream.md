# mtagent v1.5 Rationale Stream Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v1 SSE 流式协议上增量加 1 + N 条 `planner.rationale` 事件——anchor 选完后 1 条「为什么挑这几个片区」+ 每天 day_done 后 1 条「为什么这天这样编排」——用纯函数模板生成中文骨架，前端在 reveal 段和每个 day card 内显示 `💭` 浅灰小字，让评委看到 Agent 的决策逻辑。

**Architecture:** 新增 `agents/rationale.py` 提供两个 pure function（无副作用、无 LLM）；`api/routes.py::plan_stream` 在 `planner.anchors` 后调用 `build_rationale_for_anchors`、每个 `planner.day_done` 后调用 `build_rationale_for_day` 各 yield 一个 `planner.rationale` 事件；前端 `handleEvent` 加 `planner.rationale` case，render 到 reveal-section 浅灰行 + day card 顶部。Planner 类内部不感知 rationale，外科手术式增量。

**Tech Stack:** Python 3.11 + FastAPI + pytest（既有），无新依赖。前端 vanilla JS + Tailwind CDN（既有）。

**Spec reference:** `docs/superpowers/specs/2026-05-09-mtagent-v1.5-rationale-stream-design.md`

**Prerequisites:**
- v1 实施完成（65 测试全过、浏览器端到端验收通过）
- 工作目录：`/Users/yikuaibanz1/Desktop/sth/mtagent`
- venv：`source venv/bin/activate`
- mock_server 跑在 9192：`PYTHONPATH=. uvicorn dianping.mock_server:mock_app --host 127.0.0.1 --port 9192`
- `pytest tests/ -v` 65 全过为基线

---

## File Structure (v1.5 改动)

```
mtagent/
├── agents/
│   └── rationale.py                    # NEW: 2 个 pure function generator + 内部模板
├── api/
│   └── routes.py                       # MODIFIED: 2 处 yield planner.rationale 事件
├── web/
│   └── plan_stack.html                 # MODIFIED: handleEvent + 2 个 render 函数 + pendingDayRationales map
└── tests/
    ├── test_rationale.py               # NEW: generator 7+ 单测
    └── test_sse_rationale.py           # NEW: SSE 事件位置 + 字段 schema 3+ 测试
```

**不动：** `agents/planner.py`、`agents/profiler.py`、`agents/critic.py`、`dianping/`、`api/main.py`、`api/sse.py`、`api/deps.py`、`api/stub_llm.py`。这是兼容增量。

---

## Task 1: anchors rationale generator（TDD）

**Goal:** 写 `build_rationale_for_anchors(intent, anchors) -> dict`，覆盖 4 个核心 case + 1 个兜底。

**Files:**
- Create: `agents/rationale.py`
- Create: `tests/test_rationale.py`

- [ ] **Step 1: 写第一个 failing test（家庭亲子 + 拍照 + 性价比）**

Create `tests/test_rationale.py`:

```python
"""Unit tests for agents/rationale.py — pure function rationale generators."""

from dianping.schemas import ParsedIntent
from agents.rationale import build_rationale_for_anchors


def _intent(**kwargs):
    """Build a ParsedIntent with sensible defaults for tests."""
    defaults = dict(
        city="西安",
        days=3,
        traveler_type=None,
        budget_level=None,
        pace=None,
        preferences=[],
        must_visit=[],
        avoid=[],
        start_date=None,
    )
    defaults.update(kwargs)
    return ParsedIntent(**defaults)


def test_anchors_family_with_photo_budget():
    intent = _intent(
        traveler_type="家庭亲子",
        preferences=["拍照"],
        budget_level="性价比",
    )
    anchors = [("钟楼", 34.26, 108.94), ("大悦城", 34.22, 108.96)]
    out = build_rationale_for_anchors(intent, anchors)

    assert out["stage"] == "anchors"
    assert out["day_index"] is None
    text = out["text"]
    assert "带孩子" in text
    assert "拍照" in text
    assert "钟楼" in text
    assert "大悦城" in text
    assert "控住" in text or "性价比" in text
```

- [ ] **Step 2: 运行测试确认 fail**

Run:
```bash
cd /Users/yikuaibanz1/Desktop/sth/mtagent && source venv/bin/activate && PYTHONPATH=. pytest tests/test_rationale.py::test_anchors_family_with_photo_budget -v
```
Expected: FAIL with `ImportError: cannot import name 'build_rationale_for_anchors' from 'agents.rationale'`

- [ ] **Step 3: 写最小实现让 step 1 测试通过**

Create `agents/rationale.py`:

```python
"""Rationale generator — pure function templates explaining Planner decisions.

Produces dict events conforming to v1.5 SSE schema for `planner.rationale`.
No side effects, no LLM calls. Templates are stage 1 (skeleton); v1.5.1 will
add LLM polish on top.
"""

from __future__ import annotations

from typing import Optional

from dianping.schemas import DayPlan, ParsedIntent


_TRAVELER_PHRASES = {
    "情侣": "考虑你们是情侣",
    "家庭亲子": "考虑你带孩子",
    "银发": "考虑你带长辈",
    "独行": "你一个人出行",
    "商务": "你出差行程紧",
    "朋友团": "考虑朋友团出行",
}

_TRAVELER_ANCHOR_EXPLAIN = {
    "情侣": "都是出片好地标 + 周边有氛围餐厅",
    "家庭亲子": "市中心地标 + 室内商场，避免郊区奔波",
    "银发": "节奏舒缓的人文地标",
    "独行": "灵活紧凑的城市核心区",
    "商务": "高铁/机场可达性强",
    "朋友团": "热闹氛围的核心商圈",
}

_PREFERENCE_KEYWORDS = {
    "拍照": "拍照",
    "打卡": "拍照",
    "出片": "拍照",
    "美食": "爱吃",
    "文化": "喜欢人文",
    "历史": "喜欢人文",
    "小众": "爱小众",
    "网红": "拍照",
}

_BUDGET_TAILS = {
    "性价比": "，控住人均预算",
    "精致": "，挑了品质感的去处",
}


def _preference_phrase(preferences: list[str]) -> str:
    if not preferences:
        return ""
    seen: list[str] = []
    for p in preferences:
        mapped = _PREFERENCE_KEYWORDS.get(p)
        if mapped and mapped not in seen:
            seen.append(mapped)
    if not seen:
        return ""
    return "+" + "+".join(seen)


def build_rationale_for_anchors(
    intent: ParsedIntent,
    anchors: list[tuple[str, float, float]],
) -> dict:
    """Anchor-stage rationale dict per spec §5.1."""
    if not anchors:
        text = "我帮你看了城市核心区域。"
        return {
            "stage": "anchors",
            "day_index": None,
            "text": text,
            "key_factors": [f"city={intent.city or ''}"],
        }

    traveler_phrase = _TRAVELER_PHRASES.get(
        intent.traveler_type or "", "结合你的偏好"
    )
    pref_phrase = _preference_phrase(intent.preferences or [])

    names = [a[0] for a in anchors]
    if len(names) > 3:
        anchor_names = " / ".join(names[:3]) + " 等"
    else:
        anchor_names = " / ".join(names)

    explain = _TRAVELER_ANCHOR_EXPLAIN.get(
        intent.traveler_type or "", "城市的核心商圈"
    )
    budget_tail = _BUDGET_TAILS.get(intent.budget_level or "", "")

    text = f"{traveler_phrase}{pref_phrase}，我挑了 {anchor_names}——{explain}{budget_tail}。"

    factors = [
        f"traveler_type={intent.traveler_type or ''}",
        f"city={intent.city or ''}",
        f"anchors={','.join(names)}",
    ]
    if pref_phrase:
        factors.append(f"preferences={','.join(intent.preferences)}")
    if intent.budget_level:
        factors.append(f"budget_level={intent.budget_level}")

    return {
        "stage": "anchors",
        "day_index": None,
        "text": text,
        "key_factors": factors,
    }
```

- [ ] **Step 4: 运行 step 1 测试确认通过**

Run:
```bash
PYTHONPATH=. pytest tests/test_rationale.py::test_anchors_family_with_photo_budget -v
```
Expected: PASS

- [ ] **Step 5: 加剩余 4 个 anchors 测试 case**

Append to `tests/test_rationale.py`:

```python
def test_anchors_couple_food_premium():
    intent = _intent(
        city="上海",
        traveler_type="情侣",
        preferences=["美食"],
        budget_level="精致",
    )
    anchors = [("外滩", 31.24, 121.49), ("新天地", 31.22, 121.48)]
    out = build_rationale_for_anchors(intent, anchors)

    text = out["text"]
    assert "情侣" in text
    assert "爱吃" in text
    assert "外滩" in text
    assert "品质感" in text


def test_anchors_solo_no_extras():
    """No preferences and no budget — phrase should not contain '+' or budget tail."""
    intent = _intent(traveler_type="独行")
    anchors = [("钟楼", 34.26, 108.94)]
    out = build_rationale_for_anchors(intent, anchors)

    text = out["text"]
    assert "你一个人" in text
    assert "钟楼" in text
    assert "+" not in text
    assert "控住" not in text
    assert "品质感" not in text


def test_anchors_empty_list_uses_fallback():
    intent = _intent(traveler_type="情侣")
    out = build_rationale_for_anchors(intent, [])

    assert out["stage"] == "anchors"
    assert out["day_index"] is None
    assert "城市核心" in out["text"]


def test_anchors_truncates_when_more_than_three():
    intent = _intent(traveler_type="家庭亲子")
    anchors = [
        ("A", 0, 0), ("B", 0, 0), ("C", 0, 0), ("D", 0, 0), ("E", 0, 0),
    ]
    out = build_rationale_for_anchors(intent, anchors)

    text = out["text"]
    assert "A" in text and "B" in text and "C" in text
    assert "D" not in text and "E" not in text
    assert "等" in text
```

- [ ] **Step 6: 运行所有 anchors 测试确认通过**

Run:
```bash
PYTHONPATH=. pytest tests/test_rationale.py -v -k anchors
```
Expected: 5 PASS

---

## Task 2: day compose rationale generator（TDD）

**Goal:** 写 `build_rationale_for_day(intent, day_index, day_plan, anchor_name) -> dict`，覆盖 3 个核心 case + 1 个兜底。

**Files:**
- Modify: `agents/rationale.py` （加新函数）
- Modify: `tests/test_rationale.py` （加新测试）

- [ ] **Step 1: 写第一个 failing test（银发 + 3 stops）**

Append to `tests/test_rationale.py`:

```python
import datetime as _dt

from dianping.schemas import DayPlan, POI, Stop, TimeSlot
from agents.rationale import build_rationale_for_day


def _stop(poi_name: str, slot_name: str, h_start: int, h_end: int) -> Stop:
    poi = POI(
        openshopid=f"id_{poi_name}",
        name=poi_name,
        avgprice=100,
        star=4.0,
        categories=["景点"],
        latitude=0.0,
        longitude=0.0,
        city="西安",
        district="",
        review_count=0,
    )
    return Stop(
        poi=poi,
        slot=TimeSlot(
            name=slot_name,
            start=_dt.time(h_start, 0),
            end=_dt.time(h_end, 0),
        ),
        arrival_time=_dt.time(h_start, 0),
        leave_time=_dt.time(h_end, 0),
    )


def _day(stops: list[Stop], day_index: int = 0, anchor_district: str = "钟楼") -> DayPlan:
    return DayPlan(
        day_index=day_index,
        anchor_district=anchor_district,
        stops=stops,
    )


def test_day_silver_three_stops():
    intent = _intent(traveler_type="银发", days=3)
    day = _day([
        _stop("钟楼", "上午景点", 9, 12),
        _stop("回民街", "午饭", 12, 14),
        _stop("大悦城", "下午", 14, 18),
    ])
    out = build_rationale_for_day(intent, 0, day, "钟楼")

    assert out["stage"] == "compose"
    assert out["day_index"] == 0
    text = out["text"]
    assert "Day 1" in text
    assert "钟楼" in text
    assert "节奏舒缓" in text
```

- [ ] **Step 2: 运行测试确认 fail**

Run:
```bash
PYTHONPATH=. pytest tests/test_rationale.py::test_day_silver_three_stops -v
```
Expected: FAIL with `ImportError: cannot import name 'build_rationale_for_day'`

- [ ] **Step 3: 实现 `build_rationale_for_day` + 内部辅助**

Append to `agents/rationale.py`:

```python
_TRAVELER_DAY_TAIL = {
    "银发": "节奏舒缓，避免长途奔波。",
    "家庭亲子": "尽量室内 + 短间距，方便带娃。",
    "情侣": "拍照点和氛围餐厅都安排上了。",
    "独行": "节奏紧凑，单点深度体验。",
    "商务": "效率优先，集中在交通核心。",
    "朋友团": "主打热闹氛围。",
}


def _rhythm_phrase(stops: list) -> str:
    """Compose a 'morning / noon / afternoon' line from stops."""
    if not stops:
        return "暂无可推荐节奏"

    by_slot = {s.slot.name: s for s in stops}
    if len(stops) >= 4:
        morning = by_slot.get("上午景点") or stops[0]
        noon_slot = next((s for s in stops if "午" in s.slot.name and s.slot.name != "上午景点"), None)
        afternoon = next((s for s in stops if s.slot.name in ("下午", "下午茶")), None)

        parts = [f"上午 {morning.slot.name}"]
        if noon_slot is not None:
            parts.append(f"中午就近 {noon_slot.slot.name}")
        if afternoon is not None:
            parts.append(f"下午 {afternoon.slot.name}")
        return "，".join(parts)

    return "早午晚就近串成一线"


def build_rationale_for_day(
    intent: ParsedIntent,
    day_index: int,
    day_plan: DayPlan,
    anchor_name: str,
) -> dict:
    """Compose-stage rationale per spec §5.1 (one per day)."""
    if not day_plan.stops:
        return {
            "stage": "compose",
            "day_index": day_index,
            "text": f"Day {day_index + 1} 暂无可用编排。",
            "key_factors": [
                f"day_index={day_index}",
                "stops=0",
            ],
        }

    rhythm = _rhythm_phrase(day_plan.stops)
    tail = _TRAVELER_DAY_TAIL.get(intent.traveler_type or "", "")
    anchor = anchor_name or day_plan.anchor_district or "市中心"

    text = f"Day {day_index + 1} 集中在 {anchor} 附近，{rhythm}。"
    if tail:
        text += tail

    factors = [
        f"day_index={day_index}",
        f"anchor={anchor}",
        f"stops={len(day_plan.stops)}",
        f"traveler_type={intent.traveler_type or ''}",
    ]
    return {
        "stage": "compose",
        "day_index": day_index,
        "text": text,
        "key_factors": factors,
    }
```

- [ ] **Step 4: 运行 step 1 测试确认通过**

Run:
```bash
PYTHONPATH=. pytest tests/test_rationale.py::test_day_silver_three_stops -v
```
Expected: PASS

- [ ] **Step 5: 加剩余 3 个 day 测试 case**

Append to `tests/test_rationale.py`:

```python
def test_day_family_four_stops_includes_slots():
    intent = _intent(traveler_type="家庭亲子", days=2)
    day = _day([
        _stop("钟楼", "上午景点", 9, 12),
        _stop("回民街", "午饭", 12, 14),
        _stop("大悦城", "下午", 14, 17),
        _stop("永宁门", "晚饭", 18, 20),
    ], day_index=1, anchor_district="钟楼")
    out = build_rationale_for_day(intent, 1, day, "钟楼")

    text = out["text"]
    assert "Day 2" in text
    assert "钟楼" in text
    assert "上午景点" in text
    assert "午饭" in text
    assert "下午" in text
    assert "方便带娃" in text


def test_day_no_traveler_type_omits_tail():
    intent = _intent(traveler_type=None)
    day = _day([_stop("钟楼", "上午景点", 9, 12)])
    out = build_rationale_for_day(intent, 0, day, "钟楼")

    text = out["text"]
    assert "Day 1" in text
    assert "钟楼" in text
    # Tail keywords not present when traveler_type is None
    assert "方便带娃" not in text
    assert "节奏舒缓" not in text


def test_day_empty_stops_uses_fallback():
    intent = _intent(traveler_type="情侣")
    day = _day([], day_index=2)
    out = build_rationale_for_day(intent, 2, day, "钟楼")

    assert out["stage"] == "compose"
    assert out["day_index"] == 2
    assert "暂无" in out["text"]
    assert "Day 3" in out["text"]
```

- [ ] **Step 6: 运行所有 rationale 测试确认通过**

Run:
```bash
PYTHONPATH=. pytest tests/test_rationale.py -v
```
Expected: 8 PASS（5 anchors + 3 day basic + 上面新加的）

实际检查：anchors 5 + day 4（包括 step 1 那个）= 9 应全过。

- [ ] **Step 7: Commit Task 1+2**

```bash
cd /Users/yikuaibanz1/Desktop/sth/mtagent && git add agents/rationale.py tests/test_rationale.py && git commit -m "feat(v1.5): add rationale generators for anchors and day stages"
```

Expected: commit succeeds with 2 files added

---

## Task 3: SSE 集成 anchors 阶段 rationale 事件

**Goal:** 在 `routes.py` 的 `planner.anchors` 之后立即 yield `planner.rationale`（stage="anchors"）。加 SSE 协议测试断言事件位置。

**Files:**
- Modify: `api/routes.py:118-127` （anchors yield 之后插入）
- Create: `tests/test_sse_rationale.py`

- [ ] **Step 1: 写 failing SSE 协议测试（anchors 事件）**

Create `tests/test_sse_rationale.py`:

```python
"""SSE 协议测试 — planner.rationale 事件位置和字段。"""

import json

import pytest


def _parse_events(raw: str) -> list[dict]:
    """Parse SSE wire-format text into list of {event, data} dicts."""
    events = []
    blocks = raw.strip().split("\n\n")
    for block in blocks:
        ev_name = None
        ev_data = None
        for line in block.split("\n"):
            if line.startswith("event: "):
                ev_name = line[len("event: "):]
            elif line.startswith("data: "):
                ev_data = json.loads(line[len("data: "):])
        if ev_name is not None:
            events.append({"event": ev_name, "data": ev_data})
    return events


def test_anchors_rationale_emitted_after_anchors_event(sse_app_client):
    """planner.rationale (stage=anchors) must follow planner.anchors immediately."""
    resp = sse_app_client.post(
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳预算 3000 爱拍照"},
    )
    assert resp.status_code == 200

    events = _parse_events(resp.text)
    names = [e["event"] for e in events]

    # Find positions
    assert "planner.anchors" in names
    anchor_idx = names.index("planner.anchors")

    # Next event after anchors must be planner.rationale with stage=anchors
    assert anchor_idx + 1 < len(names), "no event after planner.anchors"
    next_ev = events[anchor_idx + 1]
    assert next_ev["event"] == "planner.rationale"
    assert next_ev["data"]["stage"] == "anchors"
    assert next_ev["data"]["day_index"] is None
    assert isinstance(next_ev["data"]["text"], str)
    assert len(next_ev["data"]["text"]) > 0
    assert isinstance(next_ev["data"]["key_factors"], list)
```

注意：`sse_app_client` fixture 在 `tests/conftest.py` 已存在（v1 留下）。

- [ ] **Step 2: 运行测试确认 fail**

Run:
```bash
PYTHONPATH=. pytest tests/test_sse_rationale.py::test_anchors_rationale_emitted_after_anchors_event -v
```
Expected: FAIL — assertion `next_ev["event"] == "planner.rationale"` fails，因为 routes.py 还没 yield。

- [ ] **Step 3: 改 `api/routes.py` 在 planner.anchors 后插 yield**

Read `api/routes.py:118-127` 找到这段：

```python
            yield format_event(
                "planner.anchors",
                {
                    "anchors": [
                        {"name": a[0], "lat": a[1], "lng": a[2]} for a in anchors
                    ],
                },
            )
```

紧跟之后插入：

```python
            yield format_event(
                "planner.rationale",
                build_rationale_for_anchors(intent, anchors),
            )
```

并在 `api/routes.py` 顶部 import 区加：

```python
from agents.rationale import build_rationale_for_anchors, build_rationale_for_day
```

- [ ] **Step 4: 运行 step 1 测试确认通过**

Run:
```bash
PYTHONPATH=. pytest tests/test_sse_rationale.py::test_anchors_rationale_emitted_after_anchors_event -v
```
Expected: PASS

- [ ] **Step 5: 跑 v1 既有 SSE 协议测试确认未破坏**

Run:
```bash
PYTHONPATH=. pytest tests/test_sse_protocol.py tests/test_sse_clarifying.py tests/test_sse_error.py -v
```
Expected: All previous tests still PASS（事件序列只新增不修改）

---

## Task 4: SSE 集成每日 compose rationale 事件

**Goal:** 在 routes.py 中每个 `planner.day_done` 后立即 yield `planner.rationale`（stage="compose"）。

**Files:**
- Modify: `api/routes.py:245-265` （for d in days_out 循环内 yield day_done 之后）
- Modify: `tests/test_sse_rationale.py` （加 2 个测试）

- [ ] **Step 1: 写 failing 测试（每日 rationale 事件出现 + 紧跟 day_done）**

Append to `tests/test_sse_rationale.py`:

```python
def test_day_rationale_follows_each_day_done(sse_app_client):
    """每个 planner.day_done 之后必须紧跟一个 planner.rationale (stage=compose)。"""
    resp = sse_app_client.post(
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳预算 3000 爱拍照"},
    )
    assert resp.status_code == 200

    events = _parse_events(resp.text)
    day_done_indices = [
        i for i, e in enumerate(events) if e["event"] == "planner.day_done"
    ]
    assert len(day_done_indices) >= 1, "expected at least one planner.day_done"

    for di in day_done_indices:
        assert di + 1 < len(events), f"no event after day_done at index {di}"
        next_ev = events[di + 1]
        assert next_ev["event"] == "planner.rationale", (
            f"event after day_done is {next_ev['event']}, expected planner.rationale"
        )
        assert next_ev["data"]["stage"] == "compose"
        assert next_ev["data"]["day_index"] == events[di]["data"]["day_index"]
        assert isinstance(next_ev["data"]["text"], str)
        assert len(next_ev["data"]["text"]) > 0


def test_total_rationale_count_equals_one_plus_days(sse_app_client):
    """3 天行程应有 1 anchor rationale + 3 day rationales = 4 个 rationale 事件。"""
    resp = sse_app_client.post(
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳预算 3000 爱拍照"},
    )
    events = _parse_events(resp.text)
    rationales = [e for e in events if e["event"] == "planner.rationale"]
    stages = [r["data"]["stage"] for r in rationales]

    assert stages.count("anchors") == 1
    # day_done 数量取决于 fallback 后实际生成的天数
    day_done_count = sum(1 for e in events if e["event"] == "planner.day_done")
    assert stages.count("compose") == day_done_count
```

- [ ] **Step 2: 运行测试确认 fail**

Run:
```bash
PYTHONPATH=. pytest tests/test_sse_rationale.py::test_day_rationale_follows_each_day_done -v
```
Expected: FAIL

- [ ] **Step 3: 改 `api/routes.py` 在每个 day_done 后插 yield**

Find 在 `for d in days_out:` 循环内，`yield format_event("planner.day_done", ...)` 紧跟之后（同一个 for 内、整段 yield 之外）。

具体位置（routes.py 当前 245-265）：

```python
            for d in days_out:
                yield format_event(
                    "planner.day_done",
                    {
                        ... 既有内容 ...
                    },
                )
                # ↓ 新增
                yield format_event(
                    "planner.rationale",
                    build_rationale_for_day(
                        intent,
                        d.day_index,
                        d,
                        anchors[d.day_index][0] if d.day_index < len(anchors) else "",
                    ),
                )
```

- [ ] **Step 4: 运行所有 rationale 测试确认通过**

Run:
```bash
PYTHONPATH=. pytest tests/test_sse_rationale.py -v
```
Expected: 3 PASS（anchors + day_follows + total_count）

- [ ] **Step 5: 跑全量测试确认无回归**

Run:
```bash
PYTHONPATH=. pytest tests/ -v
```
Expected: 65 (v0+v1) + 9 (rationale unit) + 3 (sse rationale) = **77 全过**

- [ ] **Step 6: Commit Task 3+4**

```bash
git add api/routes.py tests/test_sse_rationale.py && git commit -m "feat(v1.5): emit planner.rationale events after anchors and each day_done"
```

---

## Task 5: 前端集成 — handleEvent + 双段 render

**Goal:** 前端 `web/plan_stack.html` 接 `planner.rationale` 事件，分 anchors / compose 两 case render 到 reveal-section 和 day card 顶部。

**Files:**
- Modify: `web/plan_stack.html`

- [ ] **Step 1: 在 `handleEvent` 加 case + 引入 pendingDayRationales map**

Read 当前 `web/plan_stack.html:98-103` 的全局变量声明（在 `<script>` 内）。

把这段：

```javascript
    let pendingExtra = null;
```

改为：

```javascript
    let pendingExtra = null;
    const pendingDayRationales = new Map();
```

然后在 `handleEvent` switch（当前 167-243），找到 `case "planner.compose_start":` 之前或之后任意位置，加入：

```javascript
        case "planner.rationale":
          if (data.stage === "anchors") {
            renderAnchorRationale(data.text);
          } else if (data.stage === "compose") {
            pendingDayRationales.set(data.day_index, data.text);
            upsertDayRationale(data.day_index, data.text);
          }
          break;
```

- [ ] **Step 2: 加 `renderAnchorRationale` 函数**

在 `<script>` 块内（`function renderDay` 旁边），加：

```javascript
    function renderAnchorRationale(text) {
      // 在 stage-candidates 下方插浅灰小字，仅当 reveal 段还可见时插入
      const stage = document.getElementById("stage-candidates");
      if (!stage) return;
      let line = document.getElementById("anchor-rationale-line");
      if (!line) {
        line = document.createElement("div");
        line.id = "anchor-rationale-line";
        line.className = "text-stone-500 text-sm italic mt-2";
        stage.appendChild(line);
      }
      line.textContent = `💭 ${text}`;
    }
```

- [ ] **Step 3: 加 `upsertDayRationale` 函数 + 改 `renderDay` 一行**

在 `<script>` 块内，加：

```javascript
    function upsertDayRationale(dayIndex, text) {
      const card = document.querySelector(`[data-day="${dayIndex}"]`);
      if (!card) return;
      let line = card.querySelector(".day-rationale-line");
      if (!line) {
        line = document.createElement("div");
        line.className = "day-rationale-line text-stone-500 text-sm italic mb-3";
        const heading = card.querySelector("h3");
        if (heading) heading.insertAdjacentElement("afterend", line);
        else card.prepend(line);
      }
      line.textContent = `💭 ${text}`;
    }
```

并改 `renderDay` 末尾，让它读 pendingDayRationales：

把 `function renderDay(day) {` 内的最后一行 `$("timeline-days").appendChild(el);` 替换为：

```javascript
      $("timeline-days").appendChild(el);
      // 处理 rationale 事件可能先于此函数到达的情况
      const pending = pendingDayRationales.get(day.day_index);
      if (pending) upsertDayRationale(day.day_index, pending);
```

- [ ] **Step 4: 同步重置 pendingDayRationales**

`streamPlan` 函数开头清空区域时，把 pendingDayRationales 也清掉：

找到 `streamPlan(freeText, extra)` 开头那段：

```javascript
      $("understood-chips").innerHTML = "";
      $("candidate-cards").innerHTML = "";
```

后面加一行：

```javascript
      pendingDayRationales.clear();
      const oldAnchorLine = document.getElementById("anchor-rationale-line");
      if (oldAnchorLine) oldAnchorLine.remove();
```

- [ ] **Step 5: 浏览器手测路径：确认前端代码正确（语法 ok）**

Run:
```bash
cd /Users/yikuaibanz1/Desktop/sth/mtagent && python -c "
from pathlib import Path
html = Path('web/plan_stack.html').read_text()
assert 'planner.rationale' in html
assert 'pendingDayRationales' in html
assert 'renderAnchorRationale' in html
assert 'upsertDayRationale' in html
assert 'anchor-rationale-line' in html
print('frontend integration markers OK')
"
```
Expected: prints `frontend integration markers OK`

---

## Task 6: 真实 uvicorn 端到端 + curl SSE 验证 + 浏览器人眼验收

**Goal:** 真启动 uvicorn 跑流式端点，用 curl 抓 SSE 流验证 4 条 rationale 事件，然后留给用户在浏览器人眼验收。

**Files:** 无修改，仅运行验证

- [ ] **Step 1: 检查 mock_server 已在 9192 跑（Task 0 prerequisites）**

Run:
```bash
curl -s http://127.0.0.1:9192/v1/poi/search?city=%E6%B7%B1%E5%9C%B3 | head -c 100
```
Expected: JSON 开头（`{...`），不是连接拒绝

如果 mock_server 没跑，启动它：
```bash
cd /Users/yikuaibanz1/Desktop/sth/mtagent && source venv/bin/activate && PYTHONPATH=. uvicorn dianping.mock_server:mock_app --host 127.0.0.1 --port 9192 &
sleep 2
```

- [ ] **Step 2: 启动主 app on 9191（重启如已跑）**

```bash
pkill -9 -f "uvicorn api.main:app" 2>/dev/null; sleep 1
cd /Users/yikuaibanz1/Desktop/sth/mtagent && source venv/bin/activate && PYTHONPATH=. uvicorn api.main:app --host 127.0.0.1 --port 9191 > /tmp/mtagent_v15.log 2>&1 &
sleep 3
```

- [ ] **Step 3: curl POST /api/plan/stream 验证 rationale 事件**

```bash
curl -sN -X POST http://127.0.0.1:9191/api/plan/stream \
  -H "Content-Type: application/json" \
  -d '{"free_text":"情侣 3 天深圳预算 3000 爱拍照"}' \
  | grep -E "^event: planner\.(rationale|day_done|anchors)" | head -20
```

Expected: 输出包含
- 1 个 `event: planner.anchors`
- 1 个 `event: planner.rationale`（紧跟 anchors）
- 3 个 `event: planner.day_done`
- 3 个 `event: planner.rationale`（紧跟每个 day_done）

总共 8 行匹配，rationale 共 4 条。

- [ ] **Step 4: 抓出 rationale 实际文案，检查是否含关键词**

```bash
curl -sN -X POST http://127.0.0.1:9191/api/plan/stream \
  -H "Content-Type: application/json" \
  -d '{"free_text":"带长辈小孩五人西安三天预算 6000"}' \
  | grep -A1 "^event: planner.rationale" | grep "^data:"
```

Expected: 4 条 data 行（1 anchor + 3 day），文案含 "Day"、"长辈" 或 "带孩子"、"钟楼" 类关键词。

- [ ] **Step 5: 通知用户人眼浏览器验收**

输出给用户的指令：
```
后端验证通过。请在浏览器打开 http://127.0.0.1:9191/ 输入：
  "带长辈小孩五人西安三天预算 6000"
人眼检查：
  1. reveal 阶段 stage-candidates 区域有浅灰 💭 anchor 理由
  2. timeline 每个 day card 标题下有浅灰 💭 day 理由
  3. 字号小、颜色淡、不抢主体
  4. 整段流式 reveal 节奏正常（无明显卡顿）
```

- [ ] **Step 6: 用户验收完，commit**

```bash
git add web/plan_stack.html && git commit -m "feat(v1.5): render planner.rationale in reveal stage and day cards"
```

注：本 task 不更新代码，只跑验证；前端代码已在 Task 5 改完。Task 5 的 commit 实际放在这里。

---

## Self-Review

**Spec coverage（核对 spec §2 Goals 1-6）：**

| Spec Goal | Task 实现 |
|---|---|
| 1. anchors 阶段 rationale ≤80 字 + 含 anchor 名 + traveler/preference 关键词 | Task 1（含 5 个测试），Task 3（SSE 集成 + 协议测试） |
| 2. 每日 compose rationale 紧跟 day_done + 含 anchor 名 + 当日节奏 | Task 2（含 4 个测试），Task 4（SSE 集成 + 协议测试） |
| 3. 骨架 pure function | Task 1+2 实现 `agents/rationale.py` |
| 4. 前端 reveal 段显示 anchor rationale | Task 5 step 2（renderAnchorRationale） |
| 5. 前端 day card 顶部显示 day rationale | Task 5 step 3（upsertDayRationale） |
| 6. 单测覆盖 5+ case + SSE 协议测试 | Task 1+2 = 9 单测；Task 3+4 = 3 SSE 测试 |

无遗漏。

**Placeholder scan：** 无 TBD / fill-in / 模糊指令。每个 step 含具体代码或具体命令。

**Type/接口一致性：** `build_rationale_for_anchors` 输出 dict（spec §5.1 schema），`build_rationale_for_day` 输出 dict 同 schema。SSE 调用直接 `format_event("planner.rationale", build_rationale_for_xxx(...))` —— format_event 接受 dict，已验证 v1 用法。前端 `data.stage` / `data.day_index` / `data.text` 字段名一致。

**测试基线：** 65 (v0+v1) → 65 + 9 unit + 3 sse = **77 全过**为完工标志。

---

## Execution Handoff

Plan 完成，已存到：

```
docs/superpowers/specs/2026-05-09-mtagent-v1.5-rationale-stream-design.md
docs/superpowers/plans/2026-05-09-mtagent-v1.5-rationale-stream.md
```

两种执行模式：

1. **Subagent-Driven（推荐）** —— 我每个 task 派一个独立 subagent 干，每个 task 之间 review。隔离干净、推进快。需要 superpowers:subagent-driven-development。

2. **Inline Execution** —— 在当前 session 顺着 plan 一个 task 一个 task 跑，checkpoint review。需要 superpowers:executing-plans。

哪种？

# mtagent v2.5 Persona-POI Routing Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 POI 数据预标"适合什么人群 + 4 个修饰符"，运行时按人群路由检索缩小 POI 池，同时打中 < 10s 性能 + 个性化 + 创新性三个评分维度。

**Architecture:** 离线管道 (`scripts/label_pois.py`) → 标签数据 (`data/poi_labels.json`) → DianpingClient 启动加载 → `batch_get_poi` / `get_single_poi` 返回时注入 `persona_labels` → Planner 在 `batch_get_poi_details` 之后调用新 helper `tools.route_by_persona()` 做二阶段 filter+ranker → 缩小后 POI 池给后续 cluster + LLM。

**Tech Stack:** Python 3.11 / Pydantic v2 / FastAPI / pytest / pytest-asyncio。

> **Spec adjustment:** Spec section 4 says modify `tools.search_pois`. Actual implementation modifies `dianping/client.py` (label loading + attach in `batch_get_poi` / `get_single_poi`) + adds new `agents/tools.py::route_by_persona()` + adds one line in `agents/planner.py`. Reason: `search_pois` returns `SearchRecord` (lightweight, no persona_labels); only after `batch_get_poi_details` do we have full `POI` objects with `reviewTags` / `special` / `persona_labels`. This adjustment preserves spec invariants (search_pois signature unchanged, 95 tests baseline preserved).

**Spec coverage map (each spec section → which task):**
- Spec §4 architecture → Task 1+2+4+5
- Spec §5 schema changes → Task 1
- Spec §6.1 离线打标 → Task 1
- Spec §6.2 Profiler 规则兜底 → Task 3
- Spec §6.3 Search 二阶段 → Task 4
- Spec §6.4 渐进松绑 → Task 4
- Spec §7 testing strategy → Task 1-5 各分到对应测试
- Spec §11 acceptance criteria → Task 5

---

## Task 1: PersonaLabels schema + offline label pipeline

**Files:**
- Modify: `dianping/schemas.py:206-233` (新增 `PersonaLabels` 类 + 修改 `POI` + `ParsedIntent`)
- Create: `scripts/label_pois.py`
- Create: `data/poi_labels.json` (脚本产物)
- Test: `tests/test_schemas_persona.py` (新建)
- Test: `tests/test_label_pipeline.py` (新建)

### Step 1.1: 写 schema 测试 (failing)

- [ ] **Step**：创建 `tests/test_schemas_persona.py`

```python
"""Tests for PersonaLabels schema + POI/ParsedIntent backward compat."""

from dianping.schemas import (
    POI,
    ParsedIntent,
    PersonaLabels,
)


def test_persona_labels_round_trip():
    """PersonaLabels serializes and deserializes cleanly."""
    labels = PersonaLabels(
        traveler_types=["情侣", "家庭亲子"],
        modifiers={"轻量体力": True, "重文化": False, "重美食": True, "怕排队": False},
    )
    data = labels.model_dump()
    restored = PersonaLabels(**data)
    assert restored == labels


def test_poi_persona_labels_optional():
    """POI without persona_labels stays valid (backward compat)."""
    poi = POI(openshopid="abc", name="x", city="西安", latitude=34.0, longitude=108.0)
    assert poi.persona_labels is None  # default


def test_parsed_intent_modifiers_default_empty():
    """ParsedIntent without modifiers defaults to empty dict."""
    intent = ParsedIntent(city="西安", days=3, traveler_type="情侣")
    assert intent.modifiers == {}
```

### Step 1.2: 跑测试确认失败

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_schemas_persona.py -v`
- [ ] **Expected**：FAIL — `ImportError: cannot import name 'PersonaLabels'`

### Step 1.3: 实现 schema

- [ ] **Modify**：`dianping/schemas.py`，在 line 207 (`TravelerType = Literal[...]` 之后) 加：

```python
ModifierName = Literal["轻量体力", "重文化", "重美食", "怕排队"]


class PersonaLabels(BaseModel):
    """v2.5: Per-POI labels for persona routing."""

    traveler_types: list[TravelerType] = Field(default_factory=list)
    modifiers: dict[ModifierName, bool] = Field(default_factory=dict)
```

- [ ] **Modify**：`dianping/schemas.py:182` (POI 类 `min_stay_minutes` 之前)，加 1 个字段：

```python
    # --- v2.5 persona routing ---
    persona_labels: Optional["PersonaLabels"] = None
```

- [ ] **Modify**：`dianping/schemas.py:226` (ParsedIntent `start_date` 之后)，加 1 个字段：

```python
    modifiers: dict[ModifierName, bool] = Field(default_factory=dict)
```

### Step 1.4: 跑测试确认通过

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_schemas_persona.py -v`
- [ ] **Expected**：3 PASS

### Step 1.5: 跑全量回归

- [ ] **Run**：`PYTHONPATH=. pytest tests/ -v`
- [ ] **Expected**：95 + 3 = **98 PASS**（确认原 95 不破坏）

### Step 1.6: 写 label_pipeline 测试

- [ ] **Create**：`tests/test_label_pipeline.py`

```python
"""Tests for offline label pipeline rules."""

from scripts.label_pois import label_modifiers, label_traveler_types


def _poi(*, review_tags=None, special=None, queueable=False, isBlackPearl=0):
    return {
        "reviewTags": [{"tag": t, "hit": 5} for t in (review_tags or [])],
        "special": special or [],
        "queueable": queueable,
        "isBlackPearl": isBlackPearl,
    }


def test_label_traveler_types_couple():
    """'适合约会' tag → 情侣."""
    poi = _poi(review_tags=["适合约会"])
    assert "情侣" in label_traveler_types(poi)


def test_label_traveler_types_family():
    """'亲子友好' tag OR '提供婴儿椅' special → 家庭亲子."""
    assert "家庭亲子" in label_traveler_types(_poi(review_tags=["亲子友好"]))
    assert "家庭亲子" in label_traveler_types(_poi(special=["提供婴儿椅"]))


def test_label_traveler_types_fallback_solo():
    """No matched signal → ['独行'] fallback."""
    assert label_traveler_types(_poi()) == ["独行"]


def test_label_modifiers_full():
    """All 4 modifiers correctly mapped from structured fields."""
    poi = _poi(
        review_tags=["亲子友好", "老字号", "菜品精致", "等位久"],
        special=["提供婴儿椅"],
        queueable=True,
        isBlackPearl=0,
    )
    mods = label_modifiers(poi)
    assert mods["轻量体力"] is True
    assert mods["重文化"] is True
    assert mods["重美食"] is True
    assert mods["怕排队"] is False  # queueable=True AND 等位久 → 不怕排队=False
```

### Step 1.7: 跑测试确认失败

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_label_pipeline.py -v`
- [ ] **Expected**：FAIL — `ModuleNotFoundError: No module named 'scripts.label_pois'`

### Step 1.8: 实现 label pipeline

- [ ] **Create**：`scripts/__init__.py` (空文件)

- [ ] **Create**：`scripts/label_pois.py`

```python
"""Offline pipeline: label POIs with traveler_types + modifiers from
structured fields (reviewTags / special / queueable / isBlackPearl).

Run:
    PYTHONPATH=. python scripts/label_pois.py
Outputs:
    data/poi_labels.json  (~360 KB)
"""

from __future__ import annotations

import json
from collections import Counter
from pathlib import Path

CITIES = ("深圳", "上海", "西安")
DATA_DIR = Path("data/mock_dianping")
OUT_PATH = Path("data/poi_labels.json")


def label_traveler_types(poi: dict) -> list[str]:
    """Map structured fields to traveler_types list. '独行' as fallback."""
    tags = {t["tag"] for t in poi.get("reviewTags") or []}
    spec = set(poi.get("special") or [])
    types: list[str] = []
    if "适合约会" in tags:
        types.append("情侣")
    if "亲子友好" in tags or "提供婴儿椅" in spec:
        types.append("家庭亲子")
    if "无障碍" in spec:
        types.append("银发")
    if "适合聚会" in tags or "可包间" in spec:
        types.append("朋友团")
    if "包厢私密" in tags or "商务宴请" in tags:
        types.append("商务")
    if not types:
        types = ["独行"]
    return types


def label_modifiers(poi: dict) -> dict[str, bool]:
    """Map structured fields to 4 binary modifier flags."""
    tags = {t["tag"] for t in poi.get("reviewTags") or []}
    spec = set(poi.get("special") or [])
    return {
        "轻量体力": "提供婴儿椅" in spec or "无障碍" in spec or "亲子友好" in tags,
        "重文化": "老字号" in tags or "本地特色" in tags,
        "重美食": "菜品精致" in tags or "食材新鲜" in tags or poi.get("isBlackPearl") == 1,
        "怕排队": not poi.get("queueable", False) and "等位久" not in tags,
    }


def main() -> None:
    output: dict[str, dict[str, dict]] = {}
    summary: dict[str, Counter] = {}
    for city in CITIES:
        path = DATA_DIR / f"{city}.json"
        pois = json.loads(path.read_text(encoding="utf-8"))
        city_labels: dict[str, dict] = {}
        type_counter: Counter = Counter()
        for poi in pois:
            shop_id = poi.get("openshopid")
            if not shop_id:
                continue
            tt = label_traveler_types(poi)
            mods = label_modifiers(poi)
            city_labels[shop_id] = {"traveler_types": tt, "modifiers": mods}
            for t in tt:
                type_counter[t] += 1
        output[city] = city_labels
        summary[city] = type_counter
        print(f"\n[{city}] {len(city_labels)} POIs labeled")
        print(f"  traveler_type 分布:")
        for t, c in sorted(type_counter.items(), key=lambda x: -x[1]):
            pct = c / len(pois) * 100
            print(f"    {t:8s} {c:4d} ({pct:5.1f}%)")
            if pct < 5.0:
                print(f"      ⚠️  覆盖率 < 5%, 调规则补 (spec §10 risk)")
    OUT_PATH.write_text(
        json.dumps(output, ensure_ascii=False, separators=(",", ":")),
        encoding="utf-8",
    )
    print(f"\n✅ wrote {OUT_PATH} ({OUT_PATH.stat().st_size / 1024:.1f} KB)")


if __name__ == "__main__":
    main()
```

### Step 1.9: 跑测试确认通过

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_label_pipeline.py -v`
- [ ] **Expected**：4 PASS

### Step 1.10: 跑离线管道生成数据

- [ ] **Run**：`PYTHONPATH=. python scripts/label_pois.py`
- [ ] **Expected**：终端输出 3 个城市分布 + `✅ wrote data/poi_labels.json (~360 KB)`
- [ ] **Verify**：`ls -lh data/poi_labels.json` 应显示文件大小 ~300-400 KB
- [ ] **Verify**：每个 traveler_type 占比 ≥ 5%（任一类 < 5% 触发风险，需调规则重跑）

### Step 1.11: 抽检 30 个 POI 标签准确率

- [ ] **Run**：

```bash
python3 -c "
import json, random
labels = json.load(open('data/poi_labels.json'))
pois = json.load(open('data/mock_dianping/西安.json'))
random.seed(42)
sample = random.sample(pois, 30)
for poi in sample:
    sid = poi['openshopid']
    if sid in labels.get('西安', {}):
        l = labels['西安'][sid]
        tags = [t['tag'] for t in poi.get('reviewTags',[])][:5]
        print(f\"  {poi.get('name','<empty>')[:20]:20s} | tags={tags} | TT={l['traveler_types']} | mods={[k for k,v in l['modifiers'].items() if v]}\")
"
```

- [ ] **Verify**：人眼扫一遍，准确率 ≥ 80%（24/30 看着合理）。<80% 时回 Step 1.8 调 `label_traveler_types` / `label_modifiers` 规则后重跑 Step 1.10。

### Step 1.12: Commit Task 1

- [ ] **Run**：

```bash
git add dianping/schemas.py scripts/__init__.py scripts/label_pois.py \
        data/poi_labels.json tests/test_schemas_persona.py tests/test_label_pipeline.py
git commit -m "feat(v2.5): persona labels schema + offline label pipeline

- 新增 PersonaLabels schema (traveler_types + 4 modifiers)
- POI.persona_labels Optional 字段 (向后兼容)
- ParsedIntent.modifiers Optional dict (默认空)
- scripts/label_pois.py: 结构化字段映射规则
- data/poi_labels.json: 2400 POI 标签 (~360 KB)
- 测试 +7 (95 → 102 PASS), 抽检 30 个 ≥ 80% 准确

Spec ref: §5 schema, §6.1 离线打标"
```

---

## Task 2: DianpingClient 加载 + attach labels

**Files:**
- Modify: `dianping/client.py:21-103` (加 `_load_labels` + 改 `batch_get_poi` / `get_single_poi`)
- Test: `tests/test_client_persona_attach.py` (新建)

### Step 2.1: 写 client attach 测试

- [ ] **Create**：`tests/test_client_persona_attach.py`

```python
"""Tests for DianpingClient persona_labels attachment."""

import json
from pathlib import Path

import pytest

from dianping.client import DianpingClient
from dianping.schemas import POI


@pytest.fixture
def labels_file(tmp_path, monkeypatch):
    """Write a tmp poi_labels.json and point client to it via cwd."""
    data = {
        "西安": {
            "shop001": {
                "traveler_types": ["情侣"],
                "modifiers": {"轻量体力": False, "重文化": False, "重美食": True, "怕排队": True},
            }
        }
    }
    # Place the labels file in tmp_path/data/poi_labels.json
    (tmp_path / "data").mkdir()
    f = tmp_path / "data" / "poi_labels.json"
    f.write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")
    monkeypatch.chdir(tmp_path)
    return f


def test_load_labels_from_disk(labels_file):
    """Client loads labels file at construction."""
    c = DianpingClient(base_url="http://example", appkey="x", secret="y", session="z")
    assert "西安" in c._labels_cache
    assert "shop001" in c._labels_cache["西安"]


def test_load_labels_missing_file_returns_empty(monkeypatch, tmp_path):
    """Missing labels file → empty cache, no crash."""
    monkeypatch.chdir(tmp_path)  # No data/poi_labels.json here
    c = DianpingClient(base_url="http://example", appkey="x", secret="y", session="z")
    assert c._labels_cache == {}


def test_attach_labels_to_poi(labels_file):
    """_attach_labels() injects persona_labels into a POI matching openshopid."""
    c = DianpingClient(base_url="http://example", appkey="x", secret="y", session="z")
    poi = POI(
        openshopid="shop001", name="x", city="西安", latitude=34.0, longitude=108.0
    )
    c._attach_labels([poi], city="西安")
    assert poi.persona_labels is not None
    assert "情侣" in poi.persona_labels.traveler_types
    assert poi.persona_labels.modifiers["重美食"] is True
```

### Step 2.2: 跑测试确认失败

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_client_persona_attach.py -v`
- [ ] **Expected**：FAIL — `_labels_cache` / `_attach_labels` 不存在

### Step 2.3: 实现 client 加载 + attach

- [ ] **Modify**：`dianping/client.py:7-15`，import 区改为：

```python
import json
import os
import time
from pathlib import Path
from typing import Optional

import httpx

from .auth import sign
from .schemas import POI, PersonaLabels, SearchRecord
```

- [ ] **Modify**：`dianping/client.py:38` 后（`__init__` 方法末尾），加：

```python
        self._labels_cache: dict[str, dict[str, dict]] = self._load_labels()
```

- [ ] **Modify**：`dianping/client.py:39` 后（`__init__` 之后，`_post` 之前），加新方法：

```python
    @staticmethod
    def _load_labels() -> dict[str, dict[str, dict]]:
        """Load data/poi_labels.json. Empty dict if file missing (graceful degrade)."""
        path = Path("data/poi_labels.json")
        if not path.exists():
            return {}
        return json.loads(path.read_text(encoding="utf-8"))

    def _attach_labels(self, pois: list[POI], city: str) -> None:
        """Inject persona_labels into each POI matching openshopid (in-place)."""
        city_labels = self._labels_cache.get(city, {})
        for poi in pois:
            label_dict = city_labels.get(poi.openshopid)
            if label_dict:
                poi.persona_labels = PersonaLabels(**label_dict)
```

### Step 2.4: 跑 client 加载测试通过

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_client_persona_attach.py -v`
- [ ] **Expected**：3 PASS

### Step 2.5: 跑全量回归

- [ ] **Run**：`PYTHONPATH=. pytest tests/ -v`
- [ ] **Expected**：102 + 3 = **105 PASS**

### Step 2.6: Commit Task 2

- [ ] **Run**：

```bash
git add dianping/client.py tests/test_client_persona_attach.py
git commit -m "feat(v2.5): DianpingClient loads + attaches persona labels

- _load_labels() reads data/poi_labels.json at __init__
- _attach_labels(pois, city) injects PersonaLabels in-place
- Missing labels file → empty cache (graceful degrade to v2 behavior)
- Tests +3 (102 → 105 PASS)

Note: batch_get_poi / get_single_poi will be wired up to call
_attach_labels in Task 4 (after route_by_persona is in place)
to keep this task narrow.

Spec ref: §4 architecture (DianpingClient lifecycle)"
```

---

## Task 3: Profiler abstracts modifiers (LLM + rule fallback)

**Files:**
- Modify: `agents/profiler.py:62-71` (run() 末尾加规则兜底调用)
- Modify: `agents/prompts/profiler.md` (加 modifiers schema 说明)
- Test: `tests/test_profiler_modifiers.py` (新建)

### Step 3.1: 写 Profiler modifiers 测试

- [ ] **Create**：`tests/test_profiler_modifiers.py`

```python
"""Tests for Profiler modifier extraction (LLM + rule fallback)."""

import json

import pytest

from agents.context import TripContext
from agents.profiler import Profiler, _apply_modifier_defaults
from dianping.schemas import ParsedIntent, UserInput


def _intent(text: str = "", traveler_type: str = "情侣", **mods) -> ParsedIntent:
    return ParsedIntent(
        city="西安", days=3, traveler_type=traveler_type, modifiers=mods
    )


def test_apply_default_elderly_implies_light_stamina():
    """traveler_type=银发 → 轻量体力 默认 True."""
    intent = _intent(traveler_type="银发")
    _apply_modifier_defaults(intent, "")
    assert intent.modifiers["轻量体力"] is True


def test_apply_default_family_implies_light_stamina():
    """traveler_type=家庭亲子 → 轻量体力 默认 True."""
    intent = _intent(traveler_type="家庭亲子")
    _apply_modifier_defaults(intent, "")
    assert intent.modifiers["轻量体力"] is True


def test_apply_keyword_culture():
    """User text mentions '博物馆' → 重文化 = True."""
    intent = _intent()
    _apply_modifier_defaults(intent, "想去博物馆看历史")
    assert intent.modifiers["重文化"] is True


def test_apply_keyword_food_and_no_queue():
    """User text mentions '美食' + '不想排队' → 重美食 + 怕排队 both True."""
    intent = _intent()
    _apply_modifier_defaults(intent, "美食打卡，但不想排队")
    assert intent.modifiers["重美食"] is True
    assert intent.modifiers["怕排队"] is True


def test_apply_unfilled_modifiers_default_false():
    """All 4 modifiers always end up with explicit bool (no missing keys)."""
    intent = _intent(traveler_type="独行")
    _apply_modifier_defaults(intent, "")
    for m in ("轻量体力", "重文化", "重美食", "怕排队"):
        assert m in intent.modifiers
        assert intent.modifiers[m] is False  # 独行无任何关键词触发
```

### Step 3.2: 跑测试确认失败

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_profiler_modifiers.py -v`
- [ ] **Expected**：FAIL — `cannot import name '_apply_modifier_defaults'`

### Step 3.3: 实现规则兜底函数

- [ ] **Modify**：`agents/profiler.py:13-15` (`from dianping.schemas import` 行)，改为：

```python
from dianping.schemas import ModifierName, ParsedIntent, ProfilerOutput
```

- [ ] **Modify**：`agents/profiler.py:19` (REQUIRED_FIELDS 之后，class Profiler 之前)，加：

```python
KEYWORD_RULES: dict[ModifierName, tuple[str, ...]] = {
    "重文化": ("博物馆", "历史", "文化", "古迹", "陕博", "碑林"),
    "重美食": ("美食", "吃", "小吃", "网红店", "陕菜", "凉皮", "肉夹馍"),
    "怕排队": ("不想排队", "省时间", "怕等", "工作日"),
}

ALL_MODIFIERS: tuple[ModifierName, ...] = ("轻量体力", "重文化", "重美食", "怕排队")


def _apply_modifier_defaults(intent: ParsedIntent, user_text: str) -> None:
    """Fill intent.modifiers from traveler_type defaults + keyword scan.

    Mutates intent in place. Always populates all 4 modifier keys with bools.
    """
    text = (user_text or "").lower()
    # Default by traveler_type (only generates light_stamina default; other
    # modifiers stay false unless keywords trigger them)
    if intent.traveler_type in ("银发", "家庭亲子"):
        intent.modifiers.setdefault("轻量体力", True)
    # Keyword scan (any-of)
    for mod, keywords in KEYWORD_RULES.items():
        if any(w.lower() in text for w in keywords):
            intent.modifiers[mod] = True
    # Backfill missing modifiers with False (explicit, never missing)
    for m in ALL_MODIFIERS:
        intent.modifiers.setdefault(m, False)
```

### Step 3.4: 跑测试确认通过

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_profiler_modifiers.py -v`
- [ ] **Expected**：5 PASS

### Step 3.5: 接入 Profiler.run()

- [ ] **Modify**：`agents/profiler.py:74` (在 `ctx.intent = understood` 之前，构建完 `understood` 之后)，加 1 行：

```python
        _apply_modifier_defaults(understood, ctx.user_input.free_text)
```

具体位置示意（line 70-75 改造前后对比）:

```python
# Before:
            avoid=data.get("avoid") or [],
        )
        ready = True

    ctx.intent = understood

# After:
            avoid=data.get("avoid") or [],
        )
        ready = True

    _apply_modifier_defaults(understood, ctx.user_input.free_text)
    ctx.intent = understood
```

注意 indentation：`_apply_modifier_defaults` 在 `ready = True` 之后、`ctx.intent` 之前，**且要在 `if missing:` 块外执行**（无论 ready 还是 missing 都要补 modifiers）。具体位置 = **`if missing` 整个 if-else 块结束之后，`ctx.intent = understood` 之前**。

### Step 3.6: 改 prompt 加 modifiers schema

- [ ] **Modify**：`agents/prompts/profiler.md:25` (`"start_date"` 行后)，在 JSON schema 闭合 `}` 前加：

替换原 schema 块为：

```markdown
```json
{
  "city": "string，必填，必须是 深圳/上海/西安 之一（v0 范围）",
  "days": "int，必填，1-7",
  "traveler_type": "情侣 / 家庭亲子 / 银发 / 独行 / 商务 / 朋友团 中的一个",
  "budget_level": "性价比 / 适中 / 精致 三选一，或 null（信息不足时）",
  "pace": "暴走 / 适中 / 佛系，或 null（缺省由 traveler_type 决定）",
  "preferences": ["拍照", "打卡", "美食", "文化", "出片", "小众", ...],
  "must_visit": ["明确说要去的地点"],
  "avoid": ["明确说不去的地点 / 类目"],
  "start_date": "YYYY-MM-DD 格式或 null",
  "modifiers": {
    "轻量体力": "bool，true=步行≤3km/天/避大量楼梯（带老带小默认 true）",
    "重文化": "bool，true=愿意博物馆/古迹深度逛 ≥1.5h",
    "重美食": "bool，true=一天≥2 顿正餐+1 网红小吃",
    "怕排队": "bool，true=排队>30 min 直接弃"
  }
}
```
```

- [ ] **Modify**：`agents/prompts/profiler.md:42` (解析规则末尾 `4. **缺失字段**` 之后)，加 1 条规则：

```markdown
5. **修饰符抽取（v2.5）**：
   - 用户明说才设 true，没说设 false（保守）
   - 银发 / 家庭亲子 traveler_type 时，`轻量体力` 默认 true（系统会兜底，prompt 中可不重复设）
```

### Step 3.7: 跑全量回归确认 Profiler 不破坏

- [ ] **Run**：`PYTHONPATH=. pytest tests/ -v`
- [ ] **Expected**：105 + 5 = **110 PASS**

### Step 3.8: Commit Task 3

- [ ] **Run**：

```bash
git add agents/profiler.py agents/prompts/profiler.md tests/test_profiler_modifiers.py
git commit -m "feat(v2.5): Profiler extracts modifiers + rule fallback

- _apply_modifier_defaults(intent, user_text) 兜底:
  - 银发 / 家庭亲子 → 轻量体力=True 默认
  - 关键词扫描: 博物馆/历史/古迹 → 重文化=True
                美食/吃/小吃 → 重美食=True
                不想排队/省时间 → 怕排队=True
  - 未命中均默认 False (explicit, never missing key)
- Profiler.run() 末尾调用兜底函数 (在 if missing/else 块外, ctx.intent 之前)
- prompts/profiler.md 加 modifiers schema 字段 + 规则 5
- 测试 +5 (105 → 110 PASS)

Spec ref: §6.2 Profiler 规则兜底"
```

---

## Task 4: tools.route_by_persona() (filter + rank + relax) + planner 接入

**Files:**
- Modify: `agents/tools.py:340` 之后 (新增 `route_by_persona` + helper)
- Modify: `agents/planner.py:159` 之后 (Planner 调用接入)
- Modify: `dianping/client.py` (`batch_get_poi` / `get_single_poi` attach labels)
- Test: `tests/test_tools_persona_routing.py` (新建)

### Step 4.1: 写 route_by_persona 测试

- [ ] **Create**：`tests/test_tools_persona_routing.py`

```python
"""Tests for two-stage persona routing in tools.py."""

import pytest

from agents.tools import (
    NEIGHBOR_TYPES,
    RELAX_ORDER,
    _filter_by_traveler_type,
    _progressive_relax,
    _rank_by_modifiers,
    _score,
    route_by_persona,
)
from dianping.schemas import POI, ParsedIntent, PersonaLabels


def _poi(shop_id: str, traveler_types: list[str], **mods) -> POI:
    full_mods = {"轻量体力": False, "重文化": False, "重美食": False, "怕排队": False, **mods}
    return POI(
        openshopid=shop_id,
        name=shop_id,
        city="西安",
        latitude=34.0,
        longitude=108.0,
        persona_labels=PersonaLabels(traveler_types=traveler_types, modifiers=full_mods),
    )


def _intent(traveler_type: str = "情侣", **mods) -> ParsedIntent:
    full_mods = {"轻量体力": False, "重文化": False, "重美食": False, "怕排队": False, **mods}
    return ParsedIntent(city="西安", days=3, traveler_type=traveler_type, modifiers=full_mods)


def test_filter_by_traveler_type_keeps_only_matching():
    pool = [
        _poi("a", ["情侣"]),
        _poi("b", ["家庭亲子"]),
        _poi("c", ["情侣", "朋友团"]),
    ]
    out = _filter_by_traveler_type(pool, "情侣")
    assert {p.openshopid for p in out} == {"a", "c"}


def test_filter_drops_pois_without_persona_labels():
    """POI without persona_labels (legacy v2) is dropped from filter."""
    legacy = POI(openshopid="legacy", name="x", city="西安", latitude=34.0, longitude=108.0)
    pool = [_poi("a", ["情侣"]), legacy]
    out = _filter_by_traveler_type(pool, "情侣")
    assert {p.openshopid for p in out} == {"a"}


def test_score_match_plus_two_mismatch_minus_one():
    """want=True+poi=True → +2; want=True+poi=False → -1; want=False → 0."""
    intent_mods = {"重文化": True, "重美食": True, "轻量体力": False, "怕排队": False}
    poi = _poi("p", ["情侣"], 重文化=True, 重美食=False, 轻量体力=True, 怕排队=False)
    # 重文化 want=T poi=T → +2
    # 重美食 want=T poi=F → -1
    # 轻量体力 want=F → 0 (regardless of poi)
    # 怕排队 want=F → 0
    assert _score(poi, intent_mods) == 1


def test_rank_orders_by_score_desc():
    intent_mods = {"重文化": True, "轻量体力": False, "重美食": False, "怕排队": False}
    pool = [
        _poi("low", ["情侣"], 重文化=False),  # score = -1
        _poi("hi", ["情侣"], 重文化=True),    # score = +2
        _poi("mid", ["情侣"]),               # no labels match this attr; want=T poi=F → -1
    ]
    out = _rank_by_modifiers(pool, intent_mods)
    assert out[0].openshopid == "hi"


def test_progressive_relax_drops_modifier_first():
    """When filter+rank yields too few candidates, drop modifiers per RELAX_ORDER."""
    # Only 2 POI matching 情侣; not enough to satisfy min_size=8
    # Should fall through Stage 1 (drop modifiers) then Stage 2 (neighbors)
    pool = [_poi(f"id{i}", ["情侣"]) for i in range(2)]
    pool += [_poi(f"alt{i}", ["朋友团"]) for i in range(10)]  # neighbor of 情侣
    intent = _intent(traveler_type="情侣", 重文化=True, 重美食=True)
    out = _progressive_relax(pool, intent, min_size=8, top_n=20)
    assert len(out) >= 8
    # All from neighbor (朋友团) since 情侣 only has 2
    assert all(p.openshopid.startswith("alt") for p in out)


def test_progressive_relax_falls_back_to_full_pool_on_no_neighbors():
    """If no neighbors satisfy min_size, return pool[:top_n]."""
    # Singleton pool, no neighbors of 独行 will help
    pool = [_poi(f"x{i}", ["商务"]) for i in range(3)]  # not 独行 nor 朋友团
    intent = _intent(traveler_type="独行")
    out = _progressive_relax(pool, intent, min_size=8, top_n=20)
    # 独行 → neighbor=朋友团 (also empty here) → Stage 3 fallback
    assert len(out) == 3  # pool[:top_n] returns all
    assert {p.openshopid for p in out} == {"x0", "x1", "x2"}


def test_route_by_persona_full_pipeline_happy_path():
    """Plenty of matching POIs → returns top-20 from Stage 1+2 only (no relax)."""
    pool = [_poi(f"id{i}", ["家庭亲子"], 轻量体力=True) for i in range(50)]
    pool += [_poi(f"other{i}", ["独行"]) for i in range(10)]
    intent = _intent(traveler_type="家庭亲子", 轻量体力=True)
    out = route_by_persona(pool, intent, min_size=8, top_n=20)
    assert len(out) == 20
    assert all(p.openshopid.startswith("id") for p in out)


def test_relax_order_constant_present():
    """RELAX_ORDER is well-defined with all 4 modifiers."""
    assert set(RELAX_ORDER) == {"重美食", "重文化", "怕排队", "轻量体力"}
    assert RELAX_ORDER[0] == "重美食"  # 美食最先松
    assert RELAX_ORDER[-1] == "轻量体力"  # 体力最后松（生理约束）
```

### Step 4.2: 跑测试确认失败

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_tools_persona_routing.py -v`
- [ ] **Expected**：FAIL — `cannot import name 'route_by_persona'`

### Step 4.3: 实现 route_by_persona helper

- [ ] **Modify**：`agents/tools.py` 末尾（line 348 之后），追加：

```python
# =================================================================
# v2.5 Persona routing
# =================================================================

NEIGHBOR_TYPES: dict[str, list[str]] = {
    "银发": ["家庭亲子"],
    "家庭亲子": ["银发"],
    "商务": ["朋友团"],
    "朋友团": ["商务", "情侣"],
    "情侣": ["朋友团"],
    "独行": ["朋友团"],
}

# Modifier relax order: drop in this order when filter yields too few.
# Reason: 轻量体力 = physical constraint (kept longest);
#         怕排队 = literal hackathon rubric phrase (kept second);
#         重文化 / 重美食 = preferences (relaxed first).
RELAX_ORDER: list[str] = ["重美食", "重文化", "怕排队", "轻量体力"]


def _filter_by_traveler_type(pois: list[POI], traveler_type: str) -> list[POI]:
    """Stage 1: keep only POIs whose persona_labels include the traveler_type."""
    return [
        p for p in pois
        if p.persona_labels and traveler_type in p.persona_labels.traveler_types
    ]


def _score(poi: POI, intent_modifiers: dict[str, bool]) -> int:
    """+2 when want=True+poi=True, -1 when want=True+poi=False, 0 otherwise."""
    if not poi.persona_labels:
        return 0
    s = 0
    for mod, want in intent_modifiers.items():
        poi_has = poi.persona_labels.modifiers.get(mod, False)
        if want and poi_has:
            s += 2
        elif want and not poi_has:
            s -= 1
        # want=False → 0 (no reward, no penalty)
    return s


def _rank_by_modifiers(pois: list[POI], intent_modifiers: dict[str, bool]) -> list[POI]:
    """Stage 2: sort POIs by _score() descending."""
    return sorted(pois, key=lambda p: _score(p, intent_modifiers), reverse=True)


def _progressive_relax(
    pois: list[POI],
    intent: ParsedIntent,
    min_size: int = 8,
    top_n: int = 20,
) -> list[POI]:
    """Fallback: relax constraints until candidate count >= min_size.

    Stage 1: drop active modifiers per RELAX_ORDER (4→3→2→1→0).
    Stage 2: try neighbor traveler_types (e.g. 银发 → 家庭亲子).
    Stage 3: return pool[:top_n] (no persona filter).
    """
    active = {m: v for m, v in intent.modifiers.items() if v}
    for mod_to_drop in RELAX_ORDER:
        if mod_to_drop in active:
            del active[mod_to_drop]
            candidates = _filter_by_traveler_type(pois, intent.traveler_type)
            candidates = _rank_by_modifiers(candidates, active)[:top_n]
            if len(candidates) >= min_size:
                return candidates

    for neighbor in NEIGHBOR_TYPES.get(intent.traveler_type, []):
        candidates = _filter_by_traveler_type(pois, neighbor)
        if len(candidates) >= min_size:
            return candidates[:top_n]

    return pois[:top_n]


def route_by_persona(
    pois: list[POI],
    intent: ParsedIntent,
    min_size: int = 8,
    top_n: int = 20,
) -> list[POI]:
    """Two-stage persona routing: filter by traveler_type, rank by modifiers,
    fall back via progressive_relax when too few candidates.

    Returns at most top_n POIs. Designed to be called between
    batch_get_poi_details and cluster_anchor_orbit in Planner.
    """
    candidates = _filter_by_traveler_type(pois, intent.traveler_type)
    candidates = _rank_by_modifiers(candidates, intent.modifiers)[:top_n]
    if len(candidates) < min_size:
        candidates = _progressive_relax(pois, intent, min_size=min_size, top_n=top_n)
    return candidates
```

### Step 4.4: 跑测试确认通过

- [ ] **Run**：`PYTHONPATH=. pytest tests/test_tools_persona_routing.py -v`
- [ ] **Expected**：8 PASS

### Step 4.5: client.batch_get_poi / get_single_poi 接入 attach

现在 `_attach_labels` 调用要接到 client 的两个 endpoint 上。

- [ ] **Modify**：`dianping/client.py:94-103`（`get_single_poi` + `batch_get_poi`），改为：

```python
    async def get_single_poi(self, openshopid: str) -> POI:
        data = await self._post("/router/poi/getsinglepoi", {"openshopid": openshopid})
        poi = POI(**data["data"])
        self._attach_labels([poi], city=poi.city)
        return poi

    async def batch_get_poi(self, ids: list[str]) -> dict[str, POI]:
        data = await self._post(
            "/router/poi/batchgetpoi",
            {"multiopenshopid": ",".join(ids)},
        )
        pois = {k: POI(**v) for k, v in data["data"].items()}
        # Group by city (single batch typically all from one city, but be safe)
        by_city: dict[str, list[POI]] = {}
        for poi in pois.values():
            by_city.setdefault(poi.city, []).append(poi)
        for city, city_pois in by_city.items():
            self._attach_labels(city_pois, city=city)
        return pois
```

### Step 4.6: 跑全量回归确认 client 改动不破坏

- [ ] **Run**：`PYTHONPATH=. pytest tests/ -v`
- [ ] **Expected**：110 + 8 = **118 PASS**（v2.5 testing 总共 +20，超 spec 预估的 20，主要是 routing 测试 8 个比 spec 估的 6 个多）。如果有红灯，先看 `test_client.py` / `test_full_mock_parse.py` 等用真 mock_server 的测试是否因 city 字段缺失导致 attach 异常 → 在 `_attach_labels` 加 city missing 防御。

### Step 4.7: planner 接入 route_by_persona

- [ ] **Modify**：`agents/planner.py:28-35`（import 块），加 `route_by_persona`：

```python
from agents.tools import (
    batch_get_poi_details,
    cluster_anchor_orbit,
    default_pace_for_traveler,
    filter_by_intent_constraints,
    generate_day_template,
    rank_by_traveler_type,
    route_by_persona,  # v2.5
    search_pois,
)
```

(注：实际 import 列表请按 planner.py 原顺序保持字母序，本块仅示意要加 `route_by_persona`)

- [ ] **Modify**：`agents/planner.py:159` 之后（`details = await batch_get_poi_details(...)` 之后，`clusters = cluster_anchor_orbit(...)` 之前），加 2 行：

```python
        # v2.5 Persona routing: shrink POI pool by traveler_type + modifiers
        pois = route_by_persona(pois, intent, min_size=8, top_n=20)
```

注意 `pois` 变量在 line 159 后是 `list(details.values())`，确认实际变量名为 `pois`。如不是 `pois`，按现状变量名调整本步赋值。

### Step 4.8: 跑全量回归确认 planner 接入不破坏

- [ ] **Run**：`PYTHONPATH=. pytest tests/ -v`
- [ ] **Expected**：118 PASS（planner 接入是 1 行调用，不增加测试，纯调用接入）

如果 `test_planner.py` 失败，常见原因：
- 测试用的 mock POI 没有 persona_labels → filter 全砍光 → relax 触发 Stage 3 兜底返回原 pool[:top_n] → 应该不破坏旧行为
- 如果还是失败，进 `test_planner.py` 看具体红灯，可能要给测试 fixture 补 `persona_labels` 或调整 `min_size` 阈值

### Step 4.9: Commit Task 4

- [ ] **Run**：

```bash
git add agents/tools.py agents/planner.py dianping/client.py \
        tests/test_tools_persona_routing.py
git commit -m "feat(v2.5): two-stage persona routing in Planner pipeline

- agents/tools.py 新增:
  - route_by_persona(pois, intent) 主入口
  - _filter_by_traveler_type / _score / _rank_by_modifiers
  - _progressive_relax (Stage 1 drop modifiers per RELAX_ORDER,
                         Stage 2 try NEIGHBOR_TYPES,
                         Stage 3 fallback to pool[:top_n])
  - RELAX_ORDER = [重美食, 重文化, 怕排队, 轻量体力] (生理约束最后松)
- DianpingClient.batch_get_poi / get_single_poi 接入 _attach_labels
- agents/planner.py 在 batch_get_poi_details 之后调一行 route_by_persona
- 测试 +8 (110 → 118 PASS)

Spec ref: §6.3 Search 二阶段, §6.4 渐进松绑"
```

---

## Task 5: End-to-end validation + acceptance demo

**Files:**
- (No new files; runs the actual demo to verify acceptance criteria)

### Step 5.1: 启动 mock_server + main app

- [ ] **Run** in 终端 1：

```bash
PYTHONPATH=. uvicorn dianping.mock_server:mock_app --host 127.0.0.1 --port 9192
```

- [ ] **Expected**：`Application startup complete. Uvicorn running on http://127.0.0.1:9192`

- [ ] **Run** in 终端 2：

```bash
set -a && source .env && set +a
unset MTAGENT_AMAP_DISABLED  # demo 模式 amap 开
PYTHONPATH=. uvicorn api.main:app --host 127.0.0.1 --port 9191
```

- [ ] **Expected**：`Application startup complete. Uvicorn running on http://127.0.0.1:9191`

### Step 5.2: 端到端跑 query 1 — 家庭亲子

- [ ] **Run**：

```bash
time curl -s -N -X POST http://127.0.0.1:9191/plan \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"带长辈小孩五人西安三天预算 6000"}' \
  > /tmp/v25_query_family.txt
```

- [ ] **Expected**：完成时间 < v2 baseline 70%（即 v2 36s → v2.5 ≤ 25s）。Tee 的输出文件含 SSE 事件流。

### Step 5.3: 端到端跑 query 2 — 情侣

- [ ] **Run**：

```bash
time curl -s -N -X POST http://127.0.0.1:9191/plan \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"带女朋友西安三天预算 6000"}' \
  > /tmp/v25_query_couple.txt
```

- [ ] **Expected**：完成时间 < 25s。

### Step 5.4: 验证 Acceptance #1 — POI 集合 Jaccard < 0.5

- [ ] **Run**：

```bash
python3 << 'EOF'
import json, re
def extract_shopids(path):
    text = open(path).read()
    # SSE event 'day.complete' 含 stops; openshopid 在 poi.openshopid
    return set(re.findall(r'"openshopid":"([^"]+)"', text))

family = extract_shopids('/tmp/v25_query_family.txt')
couple = extract_shopids('/tmp/v25_query_couple.txt')
inter = family & couple
union = family | couple
jaccard = len(inter) / max(len(union), 1)
print(f"Family POIs: {len(family)}")
print(f"Couple POIs: {len(couple)}")
print(f"Intersection: {len(inter)}")
print(f"Union: {len(union)}")
print(f"Jaccard: {jaccard:.3f}  ({'PASS ✅' if jaccard < 0.5 else 'FAIL ❌ (太多重复 POI)'})")
EOF
```

- [ ] **Expected**：Jaccard < 0.5（超过半数 POI 不同）。失败时回 Task 1 看是否 traveler_type 标签太宽。

### Step 5.5: 验证 Acceptance #2 — 性能提升 ≥ 30%

- [ ] **Verify**：Step 5.2 + 5.3 的 `time` 输出 wallclock，比对历史 v2 baseline (~36s)。如果 v2.5 ≤ 25s ✅。

### Step 5.6: 验证 Acceptance #3 — 可解释（人眼）

- [ ] **Run**：浏览器打开 `http://127.0.0.1:9191/`，输入"带长辈小孩西安三天预算 6000" 跑一次

- [ ] **Verify**：随便指一个推荐 POI（点击 stop card），能在 console / SSE 流里看到这家 POI 的 `persona_labels`，符合标签的故事（如"reviewTags 含'亲子友好' + special 含'婴儿椅' → 家庭亲子 + 轻量体力"）

### Step 5.7: 验证 Acceptance #4 — 极端 query 兜底

- [ ] **Run**：

```bash
time curl -s -N -X POST http://127.0.0.1:9191/plan \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"商务出差西安三天，要轻量体力，重文化"}' \
  > /tmp/v25_query_extreme.txt
```

- [ ] **Expected**：仍完成（不报错），输出含合理 POI（即使触发渐进松绑）。检查日志含 `Progressive relax` 痕迹（如有日志 hook）或至少返回 ≥ 3 POI/天。

### Step 5.8: 收尾 — 不需要 commit

Task 5 是验证步骤，无代码改动。但要更新 spec README 注明实施完成（可选）。

- [ ] **Optional**：在 spec 文件末尾追加：

```markdown
---
## Implementation status
- 2026-05-XX implemented per `docs/superpowers/plans/2026-05-10-mtagent-v2.5-persona-poi-routing.md`
- All 4 acceptance criteria verified
- Tests: 95 → 118 PASS
- Branch: feat/v2.5-persona-poi-routing (or feat/v1.6-streaming-per-day)
```

---

## Self-Review (run after writing all tasks)

**Spec coverage check (§ → task):**
- ✅ §1 Context → covered in Goal section
- ✅ §2 Goals → addressed by Tasks 1-5
- ✅ §3 Decisions Q-1..Q-9 → mapped in Architecture section
- ✅ §4 Architecture → Task 1 (schema/data) + Task 2 (client load) + Task 4 (router/planner) + Task 5 (e2e)
- ✅ §5 Schema → Task 1 (PersonaLabels + POI/ParsedIntent fields)
- ✅ §6.1 离线打标 → Task 1 (label_pois.py)
- ✅ §6.2 Profiler 兜底 → Task 3 (_apply_modifier_defaults)
- ✅ §6.3 Search 二阶段 → Task 4 (route_by_persona, with spec adjustment noted in header)
- ✅ §6.4 渐进松绑 → Task 4 (_progressive_relax)
- ✅ §7 Testing → Tasks 1-4 (5 test files, 23 tests vs spec's 20)
- ✅ §8 Milestones → Tasks 1-5 map 1:1
- ✅ §9 Invariants → preserved by design (every task ends with "全量回归 ≥ 95+N PASS")
- ✅ §10 Risks → Task 1 Step 1.11 抽检, Task 4 Step 4.6 注释 city 缺失防御
- ✅ §11 Acceptance → Task 5 Steps 5.4 / 5.5 / 5.6 / 5.7

**Placeholder scan:** No "TBD", "TODO", or "implement later" found. Every code step has full code. Every command step has full command + expected output.

**Type consistency:** Function / class names checked across tasks:
- `PersonaLabels` ✓ (Task 1 schema, Task 2 import, Task 4 test fixture)
- `route_by_persona` ✓ (Task 4 helper, Task 4 planner call)
- `_apply_modifier_defaults` ✓ (Task 3 function, Task 3 Profiler.run() call)
- `_attach_labels` ✓ (Task 2 method, Task 4 client.batch_get_poi call)
- `RELAX_ORDER` ✓ (Task 4 constant, Task 4 _progressive_relax usage, Task 4 test assertion)
- `NEIGHBOR_TYPES` ✓ (Task 4 constant, Task 4 _progressive_relax usage)
- `intent.modifiers` (dict[str, bool]) ✓ consistent across Tasks 1, 3, 4

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-10-mtagent-v2.5-persona-poi-routing.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task (5 tasks), review between tasks, fast iteration. Each task is self-contained TDD (red → green → commit), good fit for fresh subagents.

**2. Inline Execution** — Execute tasks in this session using executing-plans skill, batch execution with checkpoints for human review.

**Which approach?**

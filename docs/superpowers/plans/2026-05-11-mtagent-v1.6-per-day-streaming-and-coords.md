# mtagent v1.6 Per-Day Streaming + 真坐标流 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `api/routes.py` 的 compose 段从「一次性大 LLM 调用 + 串行 yield N 天 day_done」改造成「N 个并发 `compose_one_day` async tasks + `asyncio.as_completed` 流式 yield + `day_done` payload 携带坐标 + 字符流 emit `day_partial` names」，把 warm wallclock 从 38.6s 降到 ≤ 20s，并让地图侧每天 markers 按 SSE 事件流分批 drop。

**Architecture:**
- 新增：`Planner.compose_one_day(day_idx, ...) → tuple[int, DayPlan, list[dict]]`，单天 LLM stream + on_partial 回调，失败抛 `PlannerLLMError`
- 新增：`Planner._build_one_day_payload(...)`（抽自现有 `_build_compose_payload`），单天 prompt
- 新增：`_parse_partial_stops(buf: str) → list[str]`（regex 增量提 name 字段）
- 新增：`_default_qwen_stream(system, user) → AsyncIterator[str]`（stream=True 版本）
- 改造：`api/routes.py` compose 段，`asyncio.Queue` 收 partial，`as_completed` 串行 yield day_done + rationale
- 改造：`web/plan_stack.html` SSE handler 加 `case 'planner.day_partial'`（文字浮现）+ 增强 `case 'planner.day_done'`（marker drop + per-segment polyline + totals 累计）
- 失败回退：单天 LLM 抛错 → `_synthesize_fallback_route([t], [a], [c], intent)` 单天合成 + `_compute_day_transits` 复用 → emit `day_done_fallback` 再 emit 标准 `day_done`

**Tech Stack:** Python 3.11 + asyncio + openai async SDK (stream=True) + FastAPI StreamingResponse + pytest + sse-starlette. **零新依赖**。

**Spec reference:** `docs/superpowers/specs/2026-05-11-mtagent-v1.6-per-day-streaming-and-coords-design.md`

**Prerequisites:**
- 工作目录：`/Users/yikuaibanz1/Desktop/sth/mtagent`
- 当前分支：`feat/map-view`（HEAD `e52c2cb` 含 spec commit）
- venv 激活：`source venv/bin/activate`
- 测试基线：`PYTHONPATH=. venv/bin/pytest tests/ -q` 应 121 PASS + 1 known flaky（`test_e2e_shenzhen_couple_3_days`）
- `.env` 已加 `AMAP_WEB_JS_SECURITY_CODE`（map-view session 末）

**Constraints (写代码不能违反):**
- ❗ 121 PASS baseline 不破
- ❗ `DayPlan.transit_segments: list[dict] = Field(default_factory=list)` schema 不改
- ❗ `Planner.run()` 旧入口签名不动（v0/v1.5 测试用）
- ❗ `_default_qwen_call(system, user) -> str` 旧 non-stream 接口不动（v1.5 兼容）
- ❗ 旧 SSE 事件名全部保留（profiler.* / planner.compose_start / planner.compose_done / planner.day_done / planner.rationale / transit.updated / trip.complete / critic.*）
- ❗ Wander 8 浮动 SVG 不删
- ❗ /map 旧页保留

---

## File Structure (v1.6 改动)

```
mtagent/
├── agents/
│   └── planner.py                       # MODIFIED
│       ├── + class PlannerLLMError(Exception)
│       ├── + _parse_partial_stops(buf: str) -> list[str]
│       ├── + Planner._build_one_day_payload(day_idx, intent, template, anchor, cluster) -> str
│       ├── + Planner.compose_one_day(day_idx, ..., on_partial=None) -> tuple[int, DayPlan, list[dict]]
│       └── + _default_qwen_stream(system, user) -> AsyncIterator[str]
├── api/
│   └── routes.py                        # MODIFIED: compose 段 (line 209-292) 重写
├── web/
│   └── plan_stack.html                  # MODIFIED: SSE handler + case 'planner.day_partial' + 增强 'planner.day_done'
└── tests/
    ├── test_planner_per_day.py          # NEW
    ├── test_sse_streaming_per_day.py    # NEW
    └── test_e2e_streaming_per_day_smoke.py  # NEW
```

**不动：** `dianping/schemas.py`（`transit_segments` 已就绪）、`agents/profiler.py`、`agents/rationale.py`（`build_rationale_for_day` 复用）、`agents/amap.py`、`api/main.py`、`api/sse.py`、`api/stub_llm.py`、`web/map.html`（旧页保留）。

---

## Task 1: `_parse_partial_stops` regex 增量解析（TDD）

**Goal:** 写宽容 regex 提 stop name 函数，能从不完整 JSON 中提取已出现的 name 字段。

**Files:**
- Create: `tests/test_planner_per_day.py`
- Modify: `agents/planner.py`（新增函数，文件末尾）

- [ ] **Step 1: 写 failing 单测**

Create `tests/test_planner_per_day.py`:

```python
"""Unit tests for v1.6 per-day streaming + coords (parse_partial_stops, build_one_day_payload, compose_one_day)."""

import json
from unittest.mock import AsyncMock

import pytest

from agents.planner import _parse_partial_stops


def test_parse_partial_stops_extracts_names_from_complete_json():
    buf = '{"stops":[{"name":"钟楼","slot_name":"上午景点"},{"name":"回民街","slot_name":"午饭"}]}'
    assert _parse_partial_stops(buf) == ["钟楼", "回民街"]


def test_parse_partial_stops_extracts_partial_from_incomplete_json():
    buf = '{"stops":[{"name":"钟楼","slot_name":"上午景点"},{"name":"回'
    assert _parse_partial_stops(buf) == ["钟楼"]


def test_parse_partial_stops_returns_empty_on_garbage():
    assert _parse_partial_stops("not-json-at-all") == []
    assert _parse_partial_stops("") == []
    assert _parse_partial_stops('{"summary":"xxx"}') == []


def test_parse_partial_stops_tolerates_whitespace_and_newlines():
    buf = '{\n  "stops": [\n    { "name" : "兵马俑" }\n  ]'
    assert _parse_partial_stops(buf) == ["兵马俑"]


def test_parse_partial_stops_extracts_multiple_in_order():
    buf = '{"stops":[{"name":"A"},{"name":"B"},{"name":"C"}]}'
    assert _parse_partial_stops(buf) == ["A", "B", "C"]
```

- [ ] **Step 2: 跑测试验证 fail**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py -v`
Expected: `ImportError: cannot import name '_parse_partial_stops' from 'agents.planner'`

- [ ] **Step 3: 实现函数**

Edit `agents/planner.py` 末尾（在 `_default_qwen_call` 后追加）：

```python
import re as _re

_PARTIAL_NAME_RE = _re.compile(r'"name"\s*:\s*"([^"]+)"')


def _parse_partial_stops(buf: str) -> list[str]:
    """Extract stop names from an in-progress LLM JSON buffer.

    Tolerates unterminated JSON, embedded whitespace, and garbage input.
    Used by `compose_one_day`'s on_partial callback to emit
    `planner.day_partial { day_idx, names }` events during char-stream.
    """
    if not buf:
        return []
    return _PARTIAL_NAME_RE.findall(buf)
```

- [ ] **Step 4: 跑测试验证 pass**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py -v`
Expected: 5 tests PASS.

- [ ] **Step 5: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 126 PASS（121 baseline + 5 new）+ 1 known flaky.

- [ ] **Step 6: Commit**

```bash
git add agents/planner.py tests/test_planner_per_day.py
git commit -m "$(cat <<'EOF'
feat(v1.6): add _parse_partial_stops for char-stream name extraction

Tolerant regex extracts "name" fields from in-progress LLM JSON buffer.
Used by compose_one_day's on_partial callback to emit planner.day_partial.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: `Planner._build_one_day_payload` 抽出（TDD）

**Goal:** 抽出单天 prompt 构造方法，输入只含本日 anchor + 该日聚类 POI（vs 旧 N 天大 payload）。

**Files:**
- Modify: `agents/planner.py`（class Planner 内新增方法）
- Modify: `tests/test_planner_per_day.py`（追加测试）

- [ ] **Step 1: 追加 failing 单测**

Append to `tests/test_planner_per_day.py`:

```python
from dianping.client import DianpingClient
from dianping.schemas import POI, ParsedIntent, ReviewTag
from agents.planner import Planner
from agents.tools import default_pace_for_traveler, generate_day_template


def _make_intent(**kwargs):
    defaults = dict(
        city="西安",
        days=3,
        traveler_type="家庭亲子",
        budget_level="性价比",
        pace=None,
        preferences=["拍照"],
        must_visit=[],
        avoid=[],
    )
    defaults.update(kwargs)
    return ParsedIntent(**defaults)


def _make_poi(name: str, openshopid: str, categories: list[str]) -> POI:
    return POI(
        openshopid=openshopid,
        name=name,
        city="西安",
        latitude=34.26,
        longitude=108.94,
        categories=categories,
        avgprice=150,
        star=4.5,
        reviewTags=[ReviewTag(tag="好玩", hit=3)],
        business_hour="09:00-21:00",
    )


def _planner() -> Planner:
    return Planner(client=DianpingClient(base_url="http://test", appkey="x", secret="x"))


def test_build_one_day_payload_only_contains_one_day_candidates():
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    day_cluster = [_make_poi(f"poi_{i}", f"id_{i}", ["景点"]) for i in range(5)]
    other_day_cluster = [_make_poi(f"OTHER_{i}", f"oid_{i}", ["景点"]) for i in range(5)]

    payload_str = p._build_one_day_payload(
        day_idx=0,
        intent=intent,
        template=templates[0],
        anchor=anchor,
        day_cluster_pois=day_cluster,
    )
    payload = json.loads(payload_str)

    candidate_names = [c["name"] for c in payload["candidates"]]
    assert all(n.startswith("poi_") for n in candidate_names)
    assert not any(n.startswith("OTHER_") for n in candidate_names)


def test_build_one_day_payload_uses_correct_slot_template():
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    day_cluster = [_make_poi("钟楼", "id_z", ["景点"])]

    payload_str = p._build_one_day_payload(
        day_idx=2,
        intent=intent,
        template=templates[2],
        anchor=anchor,
        day_cluster_pois=day_cluster,
    )
    payload = json.loads(payload_str)

    assert payload["day_index"] == 2
    expected_slot_names = [s.name for s in templates[2].slots]
    actual_slot_names = [s["name"] for s in payload["slots"]]
    assert actual_slot_names == expected_slot_names


def test_build_one_day_payload_caps_candidates_at_30():
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    large_cluster = [_make_poi(f"poi_{i}", f"id_{i}", ["景点"]) for i in range(50)]

    payload_str = p._build_one_day_payload(
        day_idx=0,
        intent=intent,
        template=templates[0],
        anchor=anchor,
        day_cluster_pois=large_cluster,
    )
    payload = json.loads(payload_str)

    assert len(payload["candidates"]) == 30
```

- [ ] **Step 2: 跑测试验证 fail**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py -v -k "build_one_day"`
Expected: `AttributeError: 'Planner' object has no attribute '_build_one_day_payload'`

- [ ] **Step 3: 实现方法**

在 `agents/planner.py` 的 `class Planner:` 内、`_build_compose_payload` 方法之前，新增：

```python
    def _build_one_day_payload(
        self,
        day_idx: int,
        intent: "ParsedIntent",
        template: "DayTemplate",
        anchor: tuple[str, float, float],
        day_cluster_pois: list["POI"],
    ) -> str:
        """Build a single-day LLM payload (subset of _build_compose_payload).

        Caps candidates at 30 (top-ranked already). Output JSON schema mirrors
        the per-day shape inside `_build_compose_payload.days_input[i]`.
        """
        slots_input = [
            {
                "name": s.name,
                "start": s.start.strftime("%H:%M"),
                "end": s.end.strftime("%H:%M"),
                "category_pool": s.category_pool,
                "is_meal": s.is_meal,
                "min_stay_minutes": s.min_stay_minutes,
                "max_stay_minutes": s.max_stay_minutes,
            }
            for s in template.slots
        ]
        poi_brief = [
            {
                "openshopid": p.openshopid,
                "name": p.name,
                "categories": p.categories,
                "avgprice": p.avgprice,
                "star": p.star,
                "review_tags_top3": [
                    {"tag": rt.tag, "hit": rt.hit}
                    for rt in sorted(p.reviewTags, key=lambda x: -x.hit)[:3]
                ],
                "business_hour": p.business_hour,
            }
            for p in day_cluster_pois[:30]
        ]
        return json.dumps(
            {
                "intent": {
                    "city": intent.city,
                    "traveler_type": intent.traveler_type,
                    "budget_level": intent.budget_level,
                    "preferences": intent.preferences,
                    "must_visit": intent.must_visit,
                    "avoid": intent.avoid,
                },
                "day_index": day_idx,
                "anchor_district": anchor[0],
                "slots": slots_input,
                "candidates": poi_brief,
                "output_format": {
                    "stops": [
                        {"poi_openshopid": "...", "slot_name": "..."},
                    ]
                },
            },
            ensure_ascii=False,
        )
```

- [ ] **Step 4: 跑测试验证 pass**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py -v -k "build_one_day"`
Expected: 3 tests PASS.

- [ ] **Step 5: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 129 PASS（126 + 3 new）+ 1 known flaky.

- [ ] **Step 6: Commit**

```bash
git add agents/planner.py tests/test_planner_per_day.py
git commit -m "$(cat <<'EOF'
feat(v1.6): add Planner._build_one_day_payload for single-day prompt

Extracts subset of _build_compose_payload — only includes the requested
day's anchor, slot template, and ranked POI cluster (capped at 30).
Single-day payload ~1.5k tokens vs ~6k for N-day batch.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `_default_qwen_stream` + `Planner.llm_call_stream` 注入 + stub stream

**Goal:** 新增 stream=True 版本 LLM 调用，返回 async iterator of content chunks。**同时给 Planner.__init__ 加 `llm_call_stream` 注入参数，并在 stub_llm.py 加配套 stub stream，routes.py line 119 构造 Planner 时传入**——保持 stub-LLM 测试路径（无 DASHSCOPE_API_KEY 时）可用。non-stream `_default_qwen_call` 保留不动。

**Files:**
- Modify: `agents/planner.py`（追加 stream 版本 + Planner.__init__ 加 llm_call_stream 参数）
- Modify: `api/stub_llm.py`（新增 `stub_planner_llm_stream` + `resolve_planner_llm_stream`）
- Modify: `api/routes.py`（line 119 Planner 构造行传入 llm_call_stream）
- Modify: `tests/test_planner_per_day.py`（mock-based 单测）

- [ ] **Step 1: 追加 failing 单测**

Append to `tests/test_planner_per_day.py`:

```python
import asyncio
from agents.planner import _default_qwen_stream


@pytest.mark.asyncio
async def test_default_qwen_stream_yields_string_chunks(monkeypatch):
    """Verify stream wrapper yields chunks of string (not raw delta objects)."""

    class _FakeDelta:
        def __init__(self, content):
            self.content = content

    class _FakeChoice:
        def __init__(self, content):
            self.delta = _FakeDelta(content)

    class _FakeChunk:
        def __init__(self, content):
            self.choices = [_FakeChoice(content)]

    async def _fake_stream(*args, **kwargs):
        for c in ['{"st', 'ops":[{"', 'name":"A"}]}']:
            yield _FakeChunk(c)

    class _FakeCompletions:
        async def create(self, **kwargs):
            assert kwargs.get("stream") is True
            return _fake_stream()

    class _FakeChat:
        def __init__(self):
            self.completions = _FakeCompletions()

    class _FakeClient:
        def __init__(self, **kwargs):
            self.chat = _FakeChat()

    monkeypatch.setattr("openai.AsyncOpenAI", _FakeClient)

    chunks = []
    async for chunk in _default_qwen_stream("sys", "user"):
        chunks.append(chunk)

    assert "".join(chunks) == '{"stops":[{"name":"A"}]}'
```

Verify `pytest-asyncio` is installed:

Run: `PYTHONPATH=. venv/bin/python -c "import pytest_asyncio; print(pytest_asyncio.__version__)"`
Expected: prints a version. If `ModuleNotFoundError`, install: `venv/bin/pip install pytest-asyncio` then add to `requirements.txt` if present.

Verify `asyncio_mode = "auto"` or per-test `@pytest.mark.asyncio` decorator works. Check `pytest.ini` / `pyproject.toml` for `asyncio_mode`:

Run: `grep -r "asyncio_mode" pytest.ini pyproject.toml setup.cfg 2>/dev/null`
Expected: shows `asyncio_mode = "auto"`. If empty, our test uses explicit `@pytest.mark.asyncio` decorator, which works regardless.

- [ ] **Step 2: 跑测试验证 fail**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py::test_default_qwen_stream_yields_string_chunks -v`
Expected: `ImportError: cannot import name '_default_qwen_stream' from 'agents.planner'`

- [ ] **Step 3: 实现 stream 函数 + Planner.llm_call_stream 参数**

Append to `agents/planner.py`（在 `_default_qwen_call` 后）：

```python
from typing import AsyncIterator


async def _default_qwen_stream(system: str, user: str) -> AsyncIterator[str]:
    """Streaming variant of _default_qwen_call. Yields content chunks as strings.

    Used by compose_one_day for char-level partial parsing (day_partial events).
    Non-stream _default_qwen_call is preserved for v0/v1.5 backward compat.
    """
    from openai import AsyncOpenAI

    client = AsyncOpenAI(
        api_key=os.environ.get("DASHSCOPE_API_KEY", ""),
        base_url=os.environ.get(
            "QWEN_BASE_URL",
            "https://dashscope.aliyuncs.com/compatible-mode/v1",
        ),
    )
    stream = await client.chat.completions.create(
        model=os.environ.get("QWEN_MODEL", "qwen-plus"),
        messages=[
            {"role": "system", "content": system},
            {"role": "user", "content": user},
        ],
        response_format={"type": "json_object"},
        temperature=0.3,
        max_tokens=2000,
        extra_body={"enable_thinking": False},
        stream=True,
    )
    async for chunk in stream:
        if not chunk.choices:
            continue
        delta_content = chunk.choices[0].delta.content
        if delta_content:
            yield delta_content
```

Update `class Planner.__init__` to accept optional `llm_call_stream` (find current `__init__` at line ~92):

Replace:

```python
class Planner:
    def __init__(
        self,
        client: DianpingClient,
        llm_call: Optional[Callable[[str, str], Awaitable[str]]] = None,
    ):
        self.client = client
        self.llm_call = llm_call or _default_qwen_call
        self._system_prompt = (
            _PROMPT_PATH.read_text(encoding="utf-8") if _PROMPT_PATH.exists() else ""
        )
```

with:

```python
class Planner:
    def __init__(
        self,
        client: DianpingClient,
        llm_call: Optional[Callable[[str, str], Awaitable[str]]] = None,
        llm_call_stream: Optional[Callable[[str, str], AsyncIterator[str]]] = None,
    ):
        self.client = client
        self.llm_call = llm_call or _default_qwen_call
        # llm_call_stream may be None; compose_one_day late-binds to
        # module-level _default_qwen_stream so monkeypatch works in tests.
        self.llm_call_stream = llm_call_stream
        self._system_prompt = (
            _PROMPT_PATH.read_text(encoding="utf-8") if _PROMPT_PATH.exists() else ""
        )
```

- [ ] **Step 4: 添加 stub_planner_llm_stream + resolve_planner_llm_stream**

Append to `api/stub_llm.py`:

```python
from typing import AsyncIterator


async def stub_planner_llm_stream(system: str, user: str) -> AsyncIterator[str]:
    """Stream variant of stub_planner_llm. Yields one chunk with empty stops
    so compose_one_day produces a 0-stop DayPlan → triggers fallback synthesis
    in routes.py PlannerLLMError path... actually returns empty list of stops,
    not an error. Caller handles 0-stop day as a real (empty) result.
    """
    yield json.dumps({"stops": []}, ensure_ascii=False)


def resolve_planner_llm_stream() -> Callable[[str, str], "AsyncIterator[str]"]:
    """Return real qwen stream if DASHSCOPE_API_KEY is set, else stub."""
    if os.environ.get("DASHSCOPE_API_KEY"):
        from agents.planner import _default_qwen_stream

        return _default_qwen_stream
    return stub_planner_llm_stream
```

**Important**: stub returns empty stops list (not invalid JSON). This means compose_one_day succeeds with `DayPlan(stops=[])` — caller (routes.py) must detect 0-stop result and trigger `_synthesize_fallback_route` for that day. We handle this in Task 5's routes.py refactor.

- [ ] **Step 5: 改 routes.py 构造 Planner 传 llm_call_stream**

Find `api/routes.py` line 119:

```python
            planner = Planner(client=client, llm_call=planner_llm)
```

Replace with:

```python
            from api.stub_llm import resolve_planner_llm_stream
            planner = Planner(
                client=client,
                llm_call=planner_llm,
                llm_call_stream=resolve_planner_llm_stream(),
            )
```

- [ ] **Step 6: 跑测试验证 pass**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py::test_default_qwen_stream_yields_string_chunks -v`
Expected: 1 test PASS.

- [ ] **Step 7: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 130 PASS + 1 known flaky.（注：现有 SSE 测试仍跑老 compose 路径直到 Task 5 重构 routes.py，所以仍 PASS；Task 3 只加了接口，没改 routes.py 内 LLM 调用入口）

- [ ] **Step 8: Commit**

```bash
git add agents/planner.py tests/test_planner_per_day.py
git commit -m "$(cat <<'EOF'
feat(v1.6): add _default_qwen_stream for char-level LLM streaming

Stream=True wrapper around openai AsyncOpenAI returning AsyncIterator[str].
Used by compose_one_day's on_partial callback path.
Non-stream _default_qwen_call preserved for v0/v1.5 backward compat.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `Planner.compose_one_day` + `PlannerLLMError`

**Goal:** 实现单天 async compose 方法。stream=True 期间每 chunk 跑 `_parse_partial_stops`，新 name 出现就 await `on_partial(day_idx, names)`。完整 JSON 后 build DayPlan + 调 amap 算 transit_segments，返回 `(day_idx, DayPlan, list[dict])`。任何 LLM 异常包装为 `PlannerLLMError` 上抛。

**Files:**
- Modify: `agents/planner.py`（class Planner 内 + 顶层 exception）
- Modify: `tests/test_planner_per_day.py`（追加 4 测试）

- [ ] **Step 1: 追加 failing 单测**

Append to `tests/test_planner_per_day.py`:

```python
from agents.planner import PlannerLLMError
from agents.amap import AmapClient
from agents.tools import default_pace_for_traveler, generate_day_template


@pytest.mark.asyncio
async def test_compose_one_day_returns_day_plan_with_coords(monkeypatch):
    """Happy path: LLM returns valid JSON, return value has stops with longitude/latitude."""
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    cluster = [
        _make_poi("钟楼景区", "id_a", ["景点"]),
        _make_poi("回民街", "id_b", ["美食"]),
    ]

    async def _fake_stream(system, user):
        chunks = ['{"stops":[', '{"poi_openshopid":"id_a","slot_name":"', '上午景点"}]}']
        for c in chunks:
            yield c

    async def _fake_transit(day_plan, intent_arg, amap):
        return (day_plan.day_index, [{"from_index": 0, "to_index": 1, "options": {}}])

    monkeypatch.setattr("agents.planner._default_qwen_stream", _fake_stream)
    monkeypatch.setattr("api.routes._compute_day_transits", _fake_transit)

    amap = AmapClient(key="")
    try:
        day_idx, day_plan, segs = await p.compose_one_day(
            day_idx=0,
            intent=intent,
            template=templates[0],
            anchor=anchor,
            day_cluster_pois=cluster,
            amap=amap,
        )
    finally:
        await amap._client.aclose()

    assert day_idx == 0
    assert len(day_plan.stops) == 1
    assert day_plan.stops[0].poi.longitude == 108.94
    assert day_plan.stops[0].poi.latitude == 34.26
    assert isinstance(segs, list)


@pytest.mark.asyncio
async def test_compose_one_day_emits_on_partial_with_names(monkeypatch):
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    cluster = [_make_poi("钟楼景区", "id_a", ["景点"])]

    async def _fake_stream(system, user):
        # Emit name in first chunk, finalize in second
        yield '{"stops":[{"name":"钟楼景区","poi_openshopid":"id_a","'
        yield 'slot_name":"上午景点"}]}'

    async def _fake_transit(day_plan, intent_arg, amap):
        return (day_plan.day_index, [])

    monkeypatch.setattr("agents.planner._default_qwen_stream", _fake_stream)
    monkeypatch.setattr("api.routes._compute_day_transits", _fake_transit)

    partial_calls: list[tuple[int, list[str]]] = []

    async def _on_partial(day_idx, names):
        partial_calls.append((day_idx, list(names)))

    amap = AmapClient(key="")
    try:
        await p.compose_one_day(
            day_idx=1,
            intent=intent,
            template=templates[1],
            anchor=anchor,
            day_cluster_pois=cluster,
            amap=amap,
            on_partial=_on_partial,
        )
    finally:
        await amap._client.aclose()

    # First chunk contained the name → first on_partial call with ["钟楼景区"]
    assert partial_calls
    assert partial_calls[0] == (1, ["钟楼景区"])


@pytest.mark.asyncio
async def test_compose_one_day_raises_planner_llm_error_on_invalid_json(monkeypatch):
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    cluster = [_make_poi("钟楼", "id_a", ["景点"])]

    async def _fake_stream(system, user):
        yield "not a valid json at all"

    monkeypatch.setattr("agents.planner._default_qwen_stream", _fake_stream)

    amap = AmapClient(key="")
    try:
        with pytest.raises(PlannerLLMError) as exc_info:
            await p.compose_one_day(
                day_idx=0,
                intent=intent,
                template=templates[0],
                anchor=anchor,
                day_cluster_pois=cluster,
                amap=amap,
            )
    finally:
        await amap._client.aclose()

    assert exc_info.value.day_idx == 0


@pytest.mark.asyncio
async def test_compose_one_day_raises_planner_llm_error_on_stream_exception(monkeypatch):
    p = _planner()
    intent = _make_intent()
    pace = default_pace_for_traveler(intent.traveler_type)
    templates = generate_day_template(days=3, traveler_type=intent.traveler_type, pace=pace)
    anchor = ("钟楼", 34.26, 108.94)
    cluster = [_make_poi("钟楼", "id_a", ["景点"])]

    async def _fake_stream(system, user):
        if False:  # make this an async generator
            yield ""
        raise RuntimeError("dashscope 500")

    monkeypatch.setattr("agents.planner._default_qwen_stream", _fake_stream)

    amap = AmapClient(key="")
    try:
        with pytest.raises(PlannerLLMError) as exc_info:
            await p.compose_one_day(
                day_idx=2,
                intent=intent,
                template=templates[2],
                anchor=anchor,
                day_cluster_pois=cluster,
                amap=amap,
            )
    finally:
        await amap._client.aclose()

    assert exc_info.value.day_idx == 2
    assert "dashscope 500" in str(exc_info.value)
```

- [ ] **Step 2: 跑测试验证 fail**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py -v -k "compose_one_day"`
Expected: `ImportError: cannot import name 'PlannerLLMError' from 'agents.planner'`

- [ ] **Step 3: 实现 PlannerLLMError + compose_one_day**

在 `agents/planner.py` 顶层 import 区下方新增 exception class：

```python
class PlannerLLMError(Exception):
    """Single-day LLM call failed. Carries day_idx so caller can fallback per-day."""

    def __init__(self, day_idx: int, original: Exception):
        self.day_idx = day_idx
        self.original = original
        super().__init__(f"day {day_idx} LLM failed: {original!r}")
```

在 `class Planner:` 内 `_build_one_day_payload` 之后新增：

```python
    async def compose_one_day(
        self,
        day_idx: int,
        intent: "ParsedIntent",
        template: "DayTemplate",
        anchor: tuple[str, float, float],
        day_cluster_pois: list["POI"],
        amap,
        on_partial: Optional[Callable[[int, list[str]], Awaitable[None]]] = None,
    ) -> tuple[int, DayPlan, list[dict]]:
        """Single-day LLM compose with char-level streaming and on_partial callback.

        Returns (day_idx, DayPlan, transit_segments_list).
        On any failure raises PlannerLLMError(day_idx, original).
        Caller (routes.py) catches PlannerLLMError and falls back to
        _synthesize_fallback_route + _compute_day_transits.
        """
        from api.routes import _compute_day_transits  # local import: avoid circular

        payload = self._build_one_day_payload(
            day_idx=day_idx,
            intent=intent,
            template=template,
            anchor=anchor,
            day_cluster_pois=day_cluster_pois,
        )

        buf = ""
        last_names: list[str] = []
        # Late-bind: use injected llm_call_stream if provided, else module-level
        # _default_qwen_stream (monkeypatchable in tests).
        stream_fn = self.llm_call_stream if self.llm_call_stream is not None else _default_qwen_stream
        try:
            async for chunk in stream_fn(self._system_prompt, payload):
                buf += chunk
                if on_partial is not None:
                    names = _parse_partial_stops(buf)
                    if len(names) > len(last_names):
                        last_names = names
                        await on_partial(day_idx, list(names))

            try:
                llm_data = json.loads(buf)
            except json.JSONDecodeError as exc:
                raise PlannerLLMError(day_idx, exc) from exc
        except PlannerLLMError:
            raise
        except Exception as exc:
            raise PlannerLLMError(day_idx, exc) from exc

        poi_index = {p.openshopid: p for p in day_cluster_pois}
        stops: list[Stop] = []
        for s in llm_data.get("stops") or []:
            pid = s.get("poi_openshopid")
            poi = poi_index.get(pid)
            if poi is None:
                continue
            slot_name = s.get("slot_name", template.slots[0].name)
            slot_def = next(
                (slot for slot in template.slots if slot.name == slot_name),
                template.slots[0],
            )
            stops.append(
                Stop(
                    poi=poi,
                    slot=TimeSlot(
                        name=slot_name, start=slot_def.start, end=slot_def.end
                    ),
                    arrival_time=_parse_time(s.get("arrival_time"), slot_def.start),
                    leave_time=_parse_time(s.get("leave_time"), slot_def.end),
                    transport_to_next_minutes=int(
                        s.get("transport_to_next_minutes", 30)
                    ),
                )
            )

        # Detect empty-stops outcome (e.g., stub_planner_llm_stream returns
        # `{"stops":[]}` to defer to deterministic synthesis). Treat as
        # PlannerLLMError so caller's fallback path produces a real route
        # from candidate POIs.
        if not stops and day_cluster_pois:
            raise PlannerLLMError(
                day_idx,
                ValueError("LLM returned 0 stops despite non-empty candidates"),
            )

        day_plan = DayPlan(
            day_index=day_idx,
            anchor_district=anchor[0],
            stops=stops,
        )

        # Compute transit segments for this day
        _, segments = await _compute_day_transits(day_plan, intent, amap)
        return day_idx, day_plan, segments
```

Verify `Callable, Awaitable, Optional` are imported at top of file:

Run: `grep -n "from typing import" agents/planner.py | head -3`
Expected: shows existing typing import. If `Callable, Awaitable, Optional` not in it, edit the import:

```python
from typing import Callable, Awaitable, Optional, AsyncIterator
```

- [ ] **Step 4: 跑测试验证 pass**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_planner_per_day.py -v -k "compose_one_day"`
Expected: 4 tests PASS.

- [ ] **Step 5: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 134 PASS + 1 known flaky.

- [ ] **Step 6: Commit**

```bash
git add agents/planner.py tests/test_planner_per_day.py
git commit -m "$(cat <<'EOF'
feat(v1.6): add Planner.compose_one_day + PlannerLLMError

Single-day async compose with stream=True LLM call and on_partial
callback. Yields day_partial events from char-stream as names appear.
Returns (day_idx, DayPlan, transit_segments). Any LLM failure wraps as
PlannerLLMError(day_idx, original) so caller can fallback per-day.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: routes.py compose 段重构

**Goal:** 把 `api/routes.py` 第 209-292 行单次大 LLM call + 串行 yield N 天 day_done 重写成 N 个并发 `compose_one_day` async tasks + `as_completed` + asyncio.Queue 传 partial。`day_done` payload 加 `longitude` / `latitude` 字段。原 `transit.updated` 段保留兼容。

**Files:**
- Modify: `api/routes.py`（line 209-312 整段重写）

- [ ] **Step 1: 读现有 compose 段**

Run: `sed -n '195,315p' api/routes.py`
Expected: 看到从 `_stamp("cluster_done")` 到 `_stamp("amap_done")` 整段，约 110 行。

- [ ] **Step 2: 替换 compose 段为新版本**

Edit `api/routes.py`：找到从

```python
            yield format_event("planner.compose_start", {"phase": "正在编排路线..."})

            payload = planner._build_compose_payload(
```

开始到

```python
            _stamp("amap_done")
```

为止（约 105 行）。整段替换为：

```python
            yield format_event("planner.compose_start", {"phase": "正在编排路线...", "days": intent.days})

            from agents.amap import AmapClient as _AmapClient
            from agents.planner import PlannerLLMError as _PlannerLLMError
            amap = _AmapClient(key=os.environ.get("AMAP_KEY", ""))

            # ----- v1.6: per-day concurrent compose with as_completed + on_partial -----
            partial_queue: asyncio.Queue = asyncio.Queue()

            async def _on_partial(day_idx: int, names: list[str]):
                await partial_queue.put((day_idx, names))

            sem = asyncio.Semaphore(3)

            async def _wrap(d: int):
                async with sem:
                    return await planner.compose_one_day(
                        day_idx=d,
                        intent=intent,
                        template=templates[d],
                        anchor=anchors[d],
                        day_cluster_pois=ranked_clusters[d],
                        amap=amap,
                        on_partial=_on_partial,
                    )

            try:
                tasks = [asyncio.create_task(_wrap(d)) for d in range(intent.days)]
                days_out: list[DayPlan] = [None] * intent.days  # type: ignore
                segments_by_day: dict[int, list[dict]] = {}

                for fut in asyncio.as_completed(tasks):
                    # Drain any queued partials first (best-effort, may interleave)
                    while not partial_queue.empty():
                        try:
                            pd, pn = partial_queue.get_nowait()
                            yield format_event(
                                "planner.day_partial",
                                {"day_index": pd, "names": pn},
                            )
                        except asyncio.QueueEmpty:
                            break

                    try:
                        d_idx, day_plan, segs = await fut
                        fallback_reason = None
                    except _PlannerLLMError as plerr:
                        d_idx = plerr.day_idx
                        fallback_reason = str(plerr)
                        day_plan_list = _synthesize_fallback_route(
                            [templates[d_idx]],
                            [anchors[d_idx]],
                            [ranked_clusters[d_idx]],
                            intent,
                        )
                        day_plan = day_plan_list[0] if day_plan_list else DayPlan(
                            day_index=d_idx,
                            anchor_district=anchors[d_idx][0] if d_idx < len(anchors) else "",
                            stops=[],
                        )
                        day_plan.day_index = d_idx  # _synthesize sets day_index=0
                        _, segs = await _compute_day_transits(day_plan, intent, amap)
                        yield format_event(
                            "planner.day_done_fallback",
                            {"day_index": d_idx, "reason": fallback_reason},
                        )

                    days_out[d_idx] = day_plan
                    segments_by_day[d_idx] = segs

                    # Final drain before day_done
                    while not partial_queue.empty():
                        try:
                            pd, pn = partial_queue.get_nowait()
                            yield format_event(
                                "planner.day_partial",
                                {"day_index": pd, "names": pn},
                            )
                        except asyncio.QueueEmpty:
                            break

                    yield format_event(
                        "planner.day_done",
                        {
                            "day_index": day_plan.day_index,
                            "anchor_district": day_plan.anchor_district,
                            "stops": [
                                {
                                    "poi_name": s.poi.name,
                                    "poi_openshopid": s.poi.openshopid,
                                    "categories": s.poi.categories,
                                    "slot_name": s.slot.name,
                                    "arrival_time": s.arrival_time.strftime("%H:%M"),
                                    "leave_time": s.leave_time.strftime("%H:%M"),
                                    "avgprice": s.poi.avgprice,
                                    "star": s.poi.star,
                                    "longitude": s.poi.longitude,
                                    "latitude": s.poi.latitude,
                                }
                                for s in day_plan.stops
                            ],
                            "transit_segments": segs,
                        },
                    )
                    yield format_event(
                        "planner.rationale",
                        build_rationale_for_day(
                            intent,
                            day_plan.day_index,
                            day_plan,
                            anchors[day_plan.day_index][0]
                            if day_plan.day_index < len(anchors)
                            else "",
                        ),
                    )

                # Persist transit_segments into days_out (v2.5 schema)
                for d_idx, segs in segments_by_day.items():
                    if days_out[d_idx] is not None:
                        days_out[d_idx].transit_segments = segs

                # Drop any None (defensive; shouldn't happen)
                days_out = [d for d in days_out if d is not None]
            finally:
                await amap._client.aclose()
            _stamp("compose_llm_done")
            _stamp("amap_done")
            yield format_event("planner.compose_done", {})

            # ----- v2: emit transit.updated for backward compat (old clients) -----
            for d_idx, segs in segments_by_day.items():
                yield format_event(
                    "transit.updated",
                    {"day_index": d_idx, "segments": segs},
                )
```

- [ ] **Step 3: 验证 import 完整**

Run: `grep -n "^import asyncio\|from agents.planner" api/routes.py | head -5`
Expected: 看到 `import asyncio` 和 `from agents.planner import Planner, _pick_anchors, _synthesize_fallback_route`. 新加的 `_PlannerLLMError` 和 `_AmapClient` 是 local import in 函数内，不需要顶层 import.

- [ ] **Step 4: 跑现有 SSE 协议测试看回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_sse_protocol.py tests/test_sse_transit.py tests/test_sse_clarifying.py tests/test_sse_rationale.py -v`
Expected: 全 PASS（旧事件名保留，新事件 `day_partial` / `day_done_fallback` 不影响旧测试）.

如果有 fail：检查 `planner.compose_done` 事件名是否重复（旧代码无此事件，是新增的，应无冲突）；检查 `days_out` 引用是否在 `route = RouteDraft(...)` 那行还在 scope 内（应在外层 try 内，不动）.

- [ ] **Step 5: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 134 PASS + 1 known flaky（compose 段重构后所有旧测试仍过；新 day_done 含 longitude/latitude 但旧测试只断言现有字段）.

- [ ] **Step 6: Commit**

```bash
git add api/routes.py
git commit -m "$(cat <<'EOF'
feat(v1.6): refactor routes.py compose to concurrent compose_one_day

Replace single batch LLM call with N parallel compose_one_day tasks via
asyncio.as_completed + Semaphore(3) limiter. Add asyncio.Queue for
day_partial events from char-stream. day_done payload now carries
longitude/latitude per stop (B / coords requirement). transit.updated
preserved for backward compat. PlannerLLMError per-day fallback wired.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 端到端 fallback 路径单测（验证整合）

**Goal:** Stub LLM 强制返回非法 JSON 让 PlannerLLMError 触发，验证 `day_done_fallback` event + 标准 `day_done`（坐标仍含）都按预期 emit。

**Files:**
- Create: `tests/test_sse_streaming_per_day.py`

- [ ] **Step 1: 写 failing 单测**

Create `tests/test_sse_streaming_per_day.py`:

```python
"""SSE protocol tests for v1.6 per-day streaming + day_done coords."""

import json

import pytest


@pytest.fixture(scope="module")
def app_client(sse_app_client):
    return sse_app_client


def parse_sse_stream(content: bytes) -> list[dict]:
    text = content.decode("utf-8")
    events = []
    for chunk in text.split("\n\n"):
        chunk = chunk.strip()
        if not chunk:
            continue
        ev_name = None
        ev_data = None
        for line in chunk.splitlines():
            if line.startswith("event: "):
                ev_name = line[len("event: ") :]
            elif line.startswith("data: "):
                ev_data = json.loads(line[len("data: ") :])
        if ev_name is not None:
            events.append({"event": ev_name, "data": ev_data})
    return events


def test_emits_n_day_done_events_for_n_days(app_client):
    with app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳预算 3000 爱拍照"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)
    day_done = [e for e in events if e["event"] == "planner.day_done"]
    assert len(day_done) == 3
    assert sorted(e["data"]["day_index"] for e in day_done) == [0, 1, 2]


def test_day_done_payload_includes_stop_longitude_latitude(app_client):
    with app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)
    day_done = [e for e in events if e["event"] == "planner.day_done"]
    assert day_done
    for e in day_done:
        for stop in e["data"]["stops"]:
            assert "longitude" in stop and isinstance(stop["longitude"], (int, float))
            assert "latitude" in stop and isinstance(stop["latitude"], (int, float))
            assert "poi_openshopid" in stop
            assert "slot_name" in stop


def test_day_done_payload_includes_transit_segments(app_client):
    with app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)
    day_done = [e for e in events if e["event"] == "planner.day_done"]
    for e in day_done:
        assert "transit_segments" in e["data"]
        assert isinstance(e["data"]["transit_segments"], list)


def test_compose_done_emits_after_all_day_done(app_client):
    with app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)
    last_day_done_idx = max(
        (i for i, e in enumerate(events) if e["event"] == "planner.day_done"),
        default=-1,
    )
    compose_done_idx = next(
        (i for i, e in enumerate(events) if e["event"] == "planner.compose_done"),
        -1,
    )
    assert last_day_done_idx >= 0
    assert compose_done_idx > last_day_done_idx


def test_default_stub_path_triggers_fallback_per_day(sse_app_client):
    """Default sse_app_client fixture removes DASHSCOPE_API_KEY so
    resolve_planner_llm_stream() returns stub_planner_llm_stream, which
    yields '{"stops":[]}'. compose_one_day detects empty stops and raises
    PlannerLLMError → routes.py catches → emits day_done_fallback then
    standard day_done (with longitude/latitude from _synthesize_fallback_route
    which picks real POI objects from ranked_clusters)."""
    with sse_app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)
    fallback_events = [e for e in events if e["event"] == "planner.day_done_fallback"]
    day_done = [e for e in events if e["event"] == "planner.day_done"]

    assert len(fallback_events) == 3, "stub stream returns empty stops, all 3 days fallback"
    assert len(day_done) == 3
    for e in day_done:
        assert e["data"]["stops"], f"day {e['data']['day_index']} should have synthesized stops"
        for stop in e["data"]["stops"]:
            assert "longitude" in stop and "latitude" in stop
```

- [ ] **Step 2: 跑测试验证 fail（如果有的话）**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_sse_streaming_per_day.py -v`
Expected: 5 tests run. 如果 `app_client` fixture 不解析（缺 `sse_app_client`），检查 `tests/conftest.py`:

Run: `grep -n "sse_app_client" tests/conftest.py`
Expected: 看到该 fixture 定义。如果缺失，先 cat conftest.py 查看：

Run: `cat tests/conftest.py`
Verify fixture 已注册。

- [ ] **Step 3: 跑测试验证 pass**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_sse_streaming_per_day.py -v`
Expected: 5 PASS（4 happy path + 1 fallback synth path）.

- [ ] **Step 4: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 139 PASS + 1 known flaky.

- [ ] **Step 5: Commit**

```bash
git add tests/test_sse_streaming_per_day.py
git commit -m "$(cat <<'EOF'
test(v1.6): SSE protocol tests for per-day streaming + day_done coords

5 tests: N day_done for N days, longitude/latitude in stops payload,
transit_segments present, compose_done timing, fallback synthesis path.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 真 LLM PlannerLLMError fallback 集成测试

**Goal:** Mock `_default_qwen_stream` 抛 RuntimeError 模拟 dashscope 500，验证 routes.py catch PlannerLLMError → emit `day_done_fallback` → emit 标准 `day_done`（仍含坐标）.

**Files:**
- Modify: `tests/test_sse_streaming_per_day.py`（追加测试）

- [ ] **Step 1: 追加 failing 单测**

Append to `tests/test_sse_streaming_per_day.py`:

```python
def test_real_llm_failure_emits_day_done_fallback_then_day_done(
    monkeypatch, sse_app_client
):
    """When the stream LLM raises, compose_one_day wraps in PlannerLLMError,
    routes.py catches, emits day_done_fallback then standard day_done.
    Monkeypatch the stub stream to raise (covers same code path as real
    dashscope failure since both go through compose_one_day's stream_fn)."""
    import api.stub_llm as stub_llm

    async def _broken_stream(system, user):
        if False:
            yield ""
        raise RuntimeError("simulated dashscope 500")

    monkeypatch.setattr(stub_llm, "stub_planner_llm_stream", _broken_stream)

    with sse_app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)

    fallback_events = [e for e in events if e["event"] == "planner.day_done_fallback"]
    day_done = [e for e in events if e["event"] == "planner.day_done"]

    assert len(fallback_events) == 3  # all 3 days fail → all 3 fallback
    assert len(day_done) == 3
    for e in day_done:
        for stop in e["data"]["stops"]:
            assert "longitude" in stop
            assert "latitude" in stop


def test_day_partial_events_emit_during_compose(monkeypatch, sse_app_client):
    """Verify day_partial events arrive when char-stream yields names progressively.
    Monkeypatch stub_planner_llm_stream to emit name fields across multiple
    chunks; routes.py's partial_queue drains each iteration."""
    import api.stub_llm as stub_llm

    async def _slow_stream(system, user):
        # Simulate char-stream where name fields appear before closing brace.
        # Must include real poi_openshopid from mock data — use a known stable
        # ID that appears in 深圳 mock POI set. To keep test independent of
        # specific mock data, yield empty stops at end so empty-stops fallback
        # synthesizes from real candidates while day_partial events still fire.
        yield '{"stops":[{"name":"测试 A","poi_openshopid":"'
        yield 'UNKNOWN_ID_1","slot_name":"上午景点"},{"name":"测试 B","'
        yield 'poi_openshopid":"UNKNOWN_ID_2","slot_name":"午饭"}]}'

    monkeypatch.setattr(stub_llm, "stub_planner_llm_stream", _slow_stream)

    with sse_app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    events = parse_sse_stream(body)

    partial_events = [e for e in events if e["event"] == "planner.day_partial"]
    assert partial_events, "expected at least one day_partial event"
    # At least one partial must have names list non-empty
    assert any(p["data"]["names"] for p in partial_events)
    # Names should include our synthetic stops ("测试 A" / "测试 B")
    all_names = [n for p in partial_events for n in p["data"]["names"]]
    assert "测试 A" in all_names or "测试 B" in all_names
```

- [ ] **Step 2: 跑测试验证 pass（应直接 PASS，因 routes.py 已支持）**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_sse_streaming_per_day.py::test_real_llm_failure_emits_day_done_fallback_then_day_done tests/test_sse_streaming_per_day.py::test_day_partial_events_emit_during_compose -v`
Expected: 2 PASS.

如果 fail，可能是：
- `USE_STUB_PLANNER` env var 名字跟实际不一致 → `grep -n "USE_STUB" api/stub_llm.py` 查真实名字
- POI 数据里 stub LLM 仍然被走（说明 monkeypatch resolve_planner_llm 未生效）→ 把 monkeypatch target 改成 `api.routes.resolve_planner_llm`

- [ ] **Step 3: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 141 PASS + 1 known flaky.

- [ ] **Step 4: Commit**

```bash
git add tests/test_sse_streaming_per_day.py
git commit -m "$(cat <<'EOF'
test(v1.6): real-LLM fallback + day_partial integration tests

Mock _default_qwen_stream to raise → verify PlannerLLMError caught,
day_done_fallback + standard day_done both emit with coords intact.
Second test: progressive char-stream chunks emit day_partial events
during compose phase.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: e2e 真 uvicorn 性能验证 + smoke test

**Goal:** 真 mock_server + 真 uvicorn 跑 3 天 trip，记录 wallclock，断言 ≤ 25s（buffer 给 cold start，warm ≤ 20s 目标）.

**Files:**
- Create: `tests/test_e2e_streaming_per_day_smoke.py`

- [ ] **Step 1: 写 smoke 测试**

Create `tests/test_e2e_streaming_per_day_smoke.py`:

```python
"""End-to-end smoke test for v1.6 per-day streaming.

Hits real mock_server on :9192 (must be running) and asserts:
- 3 day_done events with longitude/latitude
- transit_segments persisted to /api/plan/{trip_id}
- wallclock under 25s (cold-start buffer; warm ≤ 20s target)
"""

import json
import os
import time

import pytest


@pytest.fixture(scope="module")
def app_client(sse_app_client):
    return sse_app_client


def test_e2e_3_day_trip_under_25s(app_client):
    if os.environ.get("MTAGENT_E2E_SMOKE_SKIP") == "1":
        pytest.skip("explicit skip")

    t0 = time.perf_counter()
    with app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳预算 3000 爱拍照"},
    ) as resp:
        body = b"".join(resp.iter_bytes())
    elapsed = time.perf_counter() - t0

    text = body.decode("utf-8")
    events = []
    for chunk in text.split("\n\n"):
        chunk = chunk.strip()
        if not chunk:
            continue
        ev_name = None
        ev_data = None
        for line in chunk.splitlines():
            if line.startswith("event: "):
                ev_name = line[len("event: ") :]
            elif line.startswith("data: "):
                ev_data = json.loads(line[len("data: ") :])
        if ev_name is not None:
            events.append({"event": ev_name, "data": ev_data})

    day_done = [e for e in events if e["event"] == "planner.day_done"]
    assert len(day_done) == 3
    for e in day_done:
        assert e["data"]["stops"], f"day {e['data']['day_index']} has empty stops"
        for stop in e["data"]["stops"]:
            assert "longitude" in stop
            assert "latitude" in stop

    trip_complete = next((e for e in events if e["event"] == "trip.complete"), None)
    assert trip_complete is not None

    # Under 25s in stub-LLM path. (Real LLM path tested manually via uvicorn.)
    assert elapsed < 25.0, f"e2e wallclock {elapsed:.2f}s exceeds 25s budget"


def test_get_trip_returns_persisted_transit_segments(app_client):
    with app_client.stream(
        "POST",
        "/api/plan/stream",
        json={"free_text": "情侣 3 天深圳"},
    ) as resp:
        body = b"".join(resp.iter_bytes())

    text = body.decode("utf-8")
    trip_id = None
    for chunk in text.split("\n\n"):
        if "trip.started" in chunk:
            for line in chunk.splitlines():
                if line.startswith("data: "):
                    data = json.loads(line[len("data: ") :])
                    trip_id = data.get("trip_id")
                    break
            if trip_id:
                break

    assert trip_id is not None, "trip.started event missing trip_id"

    resp = app_client.get(f"/api/plan/{trip_id}")
    assert resp.status_code == 200
    body_json = resp.json()
    days = body_json.get("draft_route", {}).get("days", [])
    assert len(days) == 3
    for d in days:
        assert "transit_segments" in d
        assert isinstance(d["transit_segments"], list)
```

- [ ] **Step 2: 跑 smoke**

Run: `PYTHONPATH=. venv/bin/pytest tests/test_e2e_streaming_per_day_smoke.py -v`
Expected: 2 PASS.

- [ ] **Step 3: 跑全量回归**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 143 PASS + 1 known flaky.

- [ ] **Step 4: 真 uvicorn 性能验证（人工，记录在 commit message）**

Manual run（不进 CI）：

```bash
# 终端 1: mock_server (已在跑则跳)
PYTHONPATH=. venv/bin/uvicorn dianping.mock_server:mock_app --host 127.0.0.1 --port 9192 > /tmp/mtagent_mock.log 2>&1 &

# 终端 2: 主 app
set -a && source .env && set +a && unset MTAGENT_AMAP_DISABLED && \
  PYTHONPATH=. venv/bin/uvicorn api.main:app --host 127.0.0.1 --port 9191 > /tmp/mtagent_app.log 2>&1 &
sleep 2

# 跑一次 warm-up trip 让 LLM client 初始化
curl -N -X POST http://127.0.0.1:9191/api/plan/stream \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"情侣 3 天西安"}' > /dev/null

# 真测 wallclock
time curl -N -X POST http://127.0.0.1:9191/api/plan/stream \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"情侣 3 天西安预算 3000 爱拍照"}' > /tmp/trip_output.txt

# 查 phases 字段确认 compose_llm_done 时间
grep -o 'phases":[^}]*}' /tmp/trip_output.txt | tail -1
```

Expected wallclock < 25s warm（target ≤ 20s）；如果超过 25s 走 Task 7's RetestLater 流程：
- 检查 dashscope 限速：若 429 错误 → Semaphore(3) 已防，看 logs
- 检查 single-day prompt 长度：`grep -o "compose payload size" /tmp/mtagent_app.log` 应见每天 ~1-2k tokens
- 如仍慢但稳定：写实测数字进 commit message，留 v1.6.1 prompt 瘦身改进

- [ ] **Step 5: Commit smoke + manual benchmark notes**

记录手测 wallclock 在 commit 里（例如 18.2s warm）:

```bash
git add tests/test_e2e_streaming_per_day_smoke.py
git commit -m "$(cat <<'EOF'
test(v1.6): e2e smoke + perf verification

Stub-LLM smoke: 3 day_done with coords, transit persisted, wallclock < 25s.
Manual real-LLM benchmark recorded below.

Manual wallclock (real qwen-plus, 3-day 西安, stub path off):
- baseline (pre-v1.6): 38.6s warm
- v1.6:               <fill in measured number, e.g. 18.2s warm>

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 前端 plan_stack.html SSE handler

**Goal:** 在 `web/plan_stack.html` 的 SSE handler 内新增 `case 'planner.day_partial'`（文字浮现）和增强 `case 'planner.day_done'`（marker drop + per-segment polyline + totals 累计）.

**Files:**
- Modify: `web/plan_stack.html`

- [ ] **Step 1: 找现有 SSE handler 位置**

Run: `grep -n "case 'planner.day_done'\|case 'planner.day_partial'\|EventSource\|addEventListener" web/plan_stack.html | head -10`
Expected: 看到现有 `planner.day_done` case 行号。

Run: `grep -n "function renderDay\|function dropMarker\|function drawPoly\|function updateTotals" web/plan_stack.html | head -10`
Expected: 看到现有 render helper 函数名（这些可能不存在；map-view session 写的是 inline 处理，不一定有命名函数）.

- [ ] **Step 2: 读 existing `case 'planner.day_done'` 段（约 30 行）**

Run: `awk '/case .planner.day_done./,/break;/' web/plan_stack.html`
Expected: 看到现有处理逻辑（map-view session 已写的：渲染 day card + 落 markers 用 trip.complete 的整批数据）.

- [ ] **Step 3: 替换 day_done handler 用 SSE 内的 stops/transit_segments 直接落 markers**

Find the existing `case 'planner.day_done':` block in `web/plan_stack.html`. Replace whole block with:

```javascript
        case 'planner.day_done': {
          const { day_index, stops, transit_segments, anchor_district } = ev.data;
          // 1. Update day card with full stop list (chat side)
          renderDayCard(day_index, anchor_district, stops);

          // 2. Drop markers with animation delay (map side)
          if (window._mapInstance && Array.isArray(stops)) {
            stops.forEach((s, i) => {
              if (s.longitude == null || s.latitude == null) return;
              setTimeout(() => {
                dropMarker(day_index, i, s.longitude, s.latitude, s.poi_name);
              }, i * 120);
            });
          }

          // 3. Draw polyline per-segment per-mode (uses transit_segments)
          if (window._mapInstance && Array.isArray(transit_segments) && transit_segments.length) {
            drawPolylineForDay(day_index, stops, transit_segments);
          }

          // 4. Update totals chip
          if (Array.isArray(transit_segments)) {
            updateTotalsChip(transit_segments);
          }
          break;
        }

        case 'planner.day_partial': {
          const { day_index, names } = ev.data;
          // Overwrite-style render: day card shows progressive stop name list
          renderDayPartialNames(day_index, names);
          break;
        }

        case 'planner.day_done_fallback': {
          const { day_index, reason } = ev.data;
          console.warn(`[day_done_fallback] day ${day_index}: ${reason}`);
          markDayAsFallback(day_index);
          break;
        }
```

If the helper functions (`renderDayCard`, `dropMarker`, `drawPolylineForDay`, `updateTotalsChip`, `renderDayPartialNames`, `markDayAsFallback`) don't exist yet, add minimal implementations near the top of the `<script>` block:

```javascript
        // ===== v1.6 stream helpers =====
        const _dayMarkers = new Map();      // day_index -> AMap.Marker[]
        const _dayPolylines = new Map();    // day_index -> AMap.Polyline[]
        let _totalsKm = 0, _totalsMin = 0;

        function renderDayPartialNames(day_index, names) {
          const card = document.querySelector(`[data-day="${day_index}"]`);
          if (!card) return;
          const stopList = card.querySelector('.stop-list') || card;
          // overwrite-style: show progressive name list
          stopList.innerHTML = names.map((n, i) =>
            `<div class="stop-row partial">${i + 1}. ${n}</div>`
          ).join('');
        }

        function renderDayCard(day_index, anchor_district, stops) {
          let card = document.querySelector(`[data-day="${day_index}"]`);
          if (!card) {
            card = document.createElement('div');
            card.className = 'day-card';
            card.dataset.day = String(day_index);
            const container = document.getElementById('day-cards-container') || document.body;
            container.appendChild(card);
          }
          card.innerHTML = `
            <div class="day-header">Day ${day_index + 1} · ${anchor_district || ''}</div>
            <div class="stop-list">
              ${stops.map((s, i) => `
                <div class="stop-row">
                  <span class="num">${i + 1}</span>
                  <span class="name">${s.poi_name}</span>
                  <span class="slot">${s.slot_name}</span>
                </div>`).join('')}
            </div>`;
        }

        function dropMarker(day_index, stop_idx, lng, lat, name) {
          if (!window._mapInstance || !window.AMap) return;
          const marker = new AMap.Marker({
            position: [lng, lat],
            map: window._mapInstance,
            content: `<div class="numbered-pin day-${day_index}">${stop_idx + 1}</div>`,
            offset: new AMap.Pixel(-12, -36),
            title: name,
          });
          if (!_dayMarkers.has(day_index)) _dayMarkers.set(day_index, []);
          _dayMarkers.get(day_index).push(marker);
        }

        function drawPolylineForDay(day_index, stops, transit_segments) {
          if (!window._mapInstance || !window.AMap) return;
          transit_segments.forEach((seg) => {
            const { from_index, to_index, recommended } = seg;
            const a = stops[from_index], b = stops[to_index];
            if (!a || !b || a.longitude == null || b.longitude == null) return;
            const ServiceCls = recommended === 'walk' ? AMap.Walking
                              : recommended === 'bicycle' ? AMap.Riding
                              : AMap.Driving;
            const svc = new ServiceCls({ map: window._mapInstance, hideMarkers: true });
            svc.search([a.longitude, a.latitude], [b.longitude, b.latitude],
              (status, result) => {
                if (status !== 'complete') {
                  console.warn(`[poly day${day_index} seg${from_index}->${to_index}] ${status}`, result);
                  return;
                }
                // AMap drew polyline onto map already via service render path
              });
          });
        }

        function updateTotalsChip(transit_segments) {
          transit_segments.forEach(seg => {
            const opts = seg.options || {};
            const rec = seg.recommended;
            const recOpt = opts[rec];
            if (recOpt) {
              _totalsKm += (recOpt.distance_km || 0);
              _totalsMin += (recOpt.minutes || 0);
            }
          });
          const chip = document.getElementById('totals-chip');
          if (chip) {
            chip.textContent = `${_totalsKm.toFixed(1)} km · ${_totalsMin} 分钟`;
          }
        }

        function markDayAsFallback(day_index) {
          const card = document.querySelector(`[data-day="${day_index}"]`);
          if (card) card.classList.add('day-fallback');
        }
```

If the existing `plan_stack.html` already has variant helper functions for marker/polyline rendering (from map-view session), prefer adapting those names instead of adding new ones — but only if the existing helpers signature matches what we need (lng/lat from stops directly). Otherwise add the v1.6 helpers above and let the old `trip.complete` integral-batch path coexist (it'll be a no-op once day_done already filled the map).

- [ ] **Step 4: 浏览器 smoke 测试**

```bash
# 服务都跑起来（mock_server + main app）
lsof -i :9191 -i :9192   # 都在则跳过启动

# 浏览器
open http://127.0.0.1:9191/
# 提交 "情侣 3 天西安预算 3000 爱拍照"
```

Verify visually in browser DevTools console:
1. Network → EventStream tab → see `planner.day_partial` events arriving during compose
2. Map → 3 sets of 4 numbered pins drop progressively (per day)
3. Per-segment polylines render in macaron style
4. Totals chip shows km/min summed

如果有 console 错（`renderDayPartialNames is not defined` 等），检查 Step 3 helpers 是否真的塞进 `<script>` 块顶部.

- [ ] **Step 5: Commit**

```bash
git add web/plan_stack.html
git commit -m "$(cat <<'EOF'
feat(v1.6): plan_stack.html SSE handler for day_partial + coords day_done

Add case 'planner.day_partial' (overwrite-style stop name list render).
Enhance 'planner.day_done' to drop markers using stop longitude/latitude
from SSE payload directly, and draw per-segment polyline per recommended
mode (drive/walk/bicycle). Update totals chip per day. Mark fallback
days visually.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: Demo verify + 收尾

**Goal:** 真 demo 端到端，checklist 全过，记录最终 wallclock。

- [ ] **Step 1: 跑全量回归确保 baseline 不破**

Run: `PYTHONPATH=. venv/bin/pytest tests/ -q`
Expected: 143 PASS + 1 known flaky (`test_e2e_shenzhen_couple_3_days` pre-existing).

- [ ] **Step 2: 真 uvicorn warm-run 性能测**

```bash
# Restart fresh
pkill -9 -f 'uvicorn.*api.main' 2>/dev/null
pkill -9 -f 'uvicorn.*mock_server' 2>/dev/null
sleep 1

PYTHONPATH=. venv/bin/uvicorn dianping.mock_server:mock_app --host 127.0.0.1 --port 9192 > /tmp/mtagent_mock.log 2>&1 &
sleep 1
set -a && source .env && set +a && unset MTAGENT_AMAP_DISABLED && \
  PYTHONPATH=. venv/bin/uvicorn api.main:app --host 127.0.0.1 --port 9191 > /tmp/mtagent_app.log 2>&1 &
sleep 2

# Warm-up
curl -sN -X POST http://127.0.0.1:9191/api/plan/stream \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"情侣 3 天西安"}' > /dev/null

# Measured run
time curl -sN -X POST http://127.0.0.1:9191/api/plan/stream \
  -H 'Content-Type: application/json' \
  -d '{"free_text":"情侣 3 天西安预算 3000 爱拍照"}' > /tmp/trip_v16.txt
```

Expected `real <Xs>` line shows wallclock. Target ≤ 20s warm.

- [ ] **Step 3: Verify SSE payload schema**

```bash
# Count day_done events
grep -c '"event":"planner.day_done"' /tmp/trip_v16.txt
# Expected: 3

# Confirm coords in day_done
grep -A1 '"event":"planner.day_done"' /tmp/trip_v16.txt | grep -c '"longitude"'
# Expected: ≥ 3 (each day_done payload has stops with longitude)

# Count day_partial events
grep -c '"event":"planner.day_partial"' /tmp/trip_v16.txt
# Expected: ≥ 3 (at least 1 per day)
```

- [ ] **Step 4: Verify /api/plan/{trip_id} 持久化 transit_segments**

```bash
TRIP_ID=$(grep -o '"trip_id":"[^"]*"' /tmp/trip_v16.txt | head -1 | cut -d'"' -f4)
curl -s "http://127.0.0.1:9191/api/plan/${TRIP_ID}" | python -m json.tool | grep -c '"transit_segments"'
# Expected: ≥ 3
```

- [ ] **Step 5: 浏览器 demo verify**

```bash
open http://127.0.0.1:9191/
# 提交 "情侣 3 天西安预算 3000 爱拍照"
# 观察：
# 1. Wander 聊天侧：stop name 逐个浮现（不再 15s 死寂后一次性出现）
# 2. 地图：每天 markers 整批 drop（4 个 pin 0/120/240/360ms 动画）
# 3. polyline 真街道（如 securityJsCode 已配好 → 见 OK）
# 4. 顶部 totals chip 累计 km/min
```

- [ ] **Step 6: Final commit + push**

如果实测数字稳定（≤ 20s warm），追加一个收尾 commit 记录 demo verify 通过：

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(v1.6): demo verify complete

Final wallclock: <X.Y>s warm (vs 38.6s baseline). All 6 verify items pass:
- 143 pytest PASS + 1 known flaky
- 3 day_done with longitude/latitude per stop
- ≥3 day_partial events per trip
- transit_segments persisted
- Browser: chat-side name 浮现 + map-side markers 整批 drop visible

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

**不 push remote 除非 user 显式要求**——branch 仍是 `feat/map-view`，最终一个 PR 包含 map-view + v1.6 + coords + day_partial 所有 commits.

---

## Post-Plan Notes

**已知未做（留下 spec 后续）：**
- Prompt 瘦身（候选 POI 砍 top 15）→ v1.6.1
- 赛题硬门槛 ≤ 10s → v1.6.1
- Adjuster (panel 底部"再调整一下") → v1.7 / v2
- 跨天约束（黑名单去重）→ 接受 v1.6 轻微重复

**回退（emergency）：**
所有改动局限在 4 个文件（planner.py / routes.py / plan_stack.html / 测试），任意时点可 `git revert` 一个 task 的 commit 回退该 step.

**性能未达 ≤ 20s 时的诊断顺序：**
1. `grep "compose_llm_done" /tmp/mtagent_app.log` 看每天 LLM 真实耗时
2. 若单天 > 18s → token 数太大，触发 Non-goal 的 prompt 瘦身（v1.6.1 提前启动）
3. 若 max 单天 ~14s 但总 ~25s → Semaphore 排队？检查 N>3 边界（hackathon 是 3 天不会触发，但用户改 days 时会）
4. 若 dashscope 429 → 限速触发，Semaphore(3) 不够，降到 (2) 或加 retry

**Self-Review 通过项：**
- [x] Spec coverage：Goal 1-10 全有对应 task（1→T2, 2→T4-T5, 3→T5, 4→T1+T3+T4+T7, 5→T8, 6→T5, 7→T6-T7, 8→T6-T8, 9→T9, 10→T9）
- [x] 无 placeholder（每 step 含真实代码/命令/expected）
- [x] 类型一致性：`_parse_partial_stops(str) -> list[str]` / `compose_one_day(...) -> tuple[int, DayPlan, list[dict]]` / `PlannerLLMError(day_idx, original)` 全 plan 一致
- [x] Schema 字段对齐真实代码（`longitude` / `latitude`，不是 spec 简写 `lng` / `lat`）
- [x] LLM 接口 `_default_qwen_call`（non-stream）vs `_default_qwen_stream`（stream）分离，向后兼容
- [x] Tests 都在 TDD 节奏（写测 → 失败 → 实现 → 通过 → commit）

---

**End of plan.**

# 模拟数据生成需求 — 美团 Hackathon 赛题 05

> **任务对象:** 数据生成 agent（独立执行）
> **关联项目:** `~/Desktop/sth/mtagent`（目标工程）+ `~/Desktop/sth/travel-agent`（参考资产）
> **截止日期:** 2026-05-15（一周内交付 v1，越早越好）
> **作者:** Banz × Claude

---

## 1. 任务一句话

为美团 Hackathon 赛题 05（"AI 本地路线智能规划"）生成**符合大众点评开放平台 POI 数据接口字段契约**的高保真模拟数据，覆盖**深圳 / 上海 / 西安**三座城市，每城 ~800 个 POI，固化为 JSON 数据资产，供下游路线规划 Agent 直接消费。

赛题官方明确说明赛道五**没有真实数据**，"可查阅 AI 后进行模拟"——所以这份模拟数据**就是项目的真实数据基础**，不是临时 mock。

---

## 2. 你能用的输入资产

### 2.1 字段契约文档（必读，是硬约束）

字段定义和接口形态见 `~/Desktop/sth/mtagent/meituan_hackathon_plan.md` 第 5 章和第 11 章。本文档第 3 节给出生成时必须遵守的字段清单。**字段名拼写、类型、嵌套结构必须 1:1 与点评 schema 对齐**——下游工程会按这个 schema 写 Pydantic 解析器，你写错一个字段名整条流水线就崩。

### 2.2 真实数据骨架（直接复用，不要从零编）

**绝对路径：**
- `~/Desktop/sth/travel-agent/data/cleaned/深圳_20260419.json`（505 条已清洗 POI，带 name/lat/lng/district/address/warnings/type）
- `~/Desktop/sth/travel-agent/data/cleaned/上海_20260415.json`
- `~/Desktop/sth/travel-agent/data/cleaned/西安_*.json`（找最新日期那份）

这些文件里的 `name` / `lat` / `lng` / `district` / `address` 是**真实存在的 POI**，你**必须**把这些当作生成的骨架——不要重新编 POI 名字和坐标，不要让"深圳·华强北"出现在罗湖区。**真实地理 grounding 是评委一眼能看出来的东西。**

如果 cleaned 数据不够 800 条，从同目录的 `~/Desktop/sth/travel-agent/data/raw/amap_*.json` 补（也是真实 POI，30 条/文件，多个文件可拼）。

### 2.3 评论语料源（LLM 生成 UGC 时的养料）

`~/Desktop/sth/travel-agent/data/raw/xhs_深圳*.json`（小红书原始笔记，主题分美食/景点/攻略/拍照/住宿/酒店等，每文件 ~50 条）。生成 `ugcs` 字段时，**先从这里读真实评论的语气、用词、关注点**，再让 LLM 仿写——不要凭空让 LLM 编"用户说环境优雅"这种 AI 味道太重的句子。上海 / 西安没有 xhs 数据，可以参考深圳的语气模板 + 联网搜索补。

### 2.4 联网搜索（补缺）

允许并鼓励：
- 三个城市的真实**商圈清单**（万象天地、陆家嘴、大唐不夜城等）—— 用作 POI 聚簇的 anchor
- 真实**地标 / 餐厅榜单**（黑珍珠、必吃榜、城市美食地图）—— 补 POI 名单 + 用作 `isBlackPearl` 字段的 grounding
- 大众点评公开页面（不爬数据，看字段呈现）—— 帮你理解真实评论标签长什么样

**禁止：** 爬大众点评 API 或登录态页面（违规，没意义）。

---

## 3. 字段契约（生成时必须遵守）

每个 POI 输出一个 JSON 对象，结构如下。**所有字段名小写下划线或驼峰按本表**——和点评接口文档保持一致，不要自创。

### 3.1 必填字段（每个 POI 都要有，不能缺）

| 字段 | 类型 | 取值规则 |
|---|---|---|
| `openshopid` | string | 16 位 base64-like 字符串，全局唯一（参考真实形式：`1IPcei6VQABtnzKwJ3pXvw`），可用 `secrets.token_urlsafe(12)` 生成 |
| `openstatus` | int | `0` 或 `1`（**95% 应为 1，5% 为 0** 模拟下线场景，让 Critic 有判断空间） |
| `name` | string | POI 主名称（来自骨架数据） |
| `branch_name` | string | 分店名，无分店则空字符串 `""` |
| `address` | string | 完整地址（来自骨架数据） |
| `city` | string | "深圳" / "上海" / "西安" |
| `latitude` | double | GCJ-02 坐标系（骨架数据已经是 GCJ-02，直接用） |
| `longitude` | double | 同上 |
| `categories` | list[string] | 叶子类目数组，用点评 search 接口的 20 类目枚举做一级类，叶子类目可细分（参考骨架的 `type` 字段映射） |
| `star` | float | 0.0-5.0，半星精度（4.0 / 4.5 / 5.0 这种），分布要合理（**大部分在 4.0-4.8**，少数 ≤ 3.5，极少数 = 0 表示无评分） |
| `reviewCount` | int | 评论总数，**和 star 正相关**：高分热门店应有 1000-50000，冷门店 50-500 |

### 3.2 关键富字段（demo 命脉，重点投入生成质量）

| 字段 | 类型 | 取值规则 |
|---|---|---|
| `avgprice` | int | 人均消费，**按类目和星级合理分布**：餐饮 30-1500、娱乐 80-500、咖啡 30-100；黑珍珠类 800+ |
| `business_hour` | string | 严格格式：`"10:00-22:00"` 或 `"10:00-14:00, 17:00-22:00"`（午休型）。**禁止**自由文本如"营业中"、"晚饭后" |
| `highquality` | int | `0` 或 `1`，约 15% 为 1（高质量标志） |
| `isBlackPearl` | int | `0` 或 `1`，**只对人均 ≥ 500 的高端餐饮**给 1，且每城不超过 30 个 |
| `dishs` | list[obj] | 餐饮类**必填 ≥ 3 道菜**，非餐饮类可空 list。每个对象：`{dishName, picUrl: "" 或 placeholder, price: float, recommendCount: int}` |
| `special` | list[string] | 商户特色服务列表，从枚举里选 0-3 个：`["免费 WiFi", "可停车", "可包间", "可刷卡", "宠物友好", "无障碍", "提供婴儿椅", "可外带"]` |
| `takeawayable` | bool | 餐饮类大部分 true，景点 / 娱乐 false |
| `queueable` | bool | 网红 / 高人气餐饮 true，普通店 false |
| `bookable` | bool | 高端餐饮 / 婚宴 / 酒店 true |
| `telephone` | string | 带区号，深圳 `0755-` / 上海 `021-` / 西安 `029-`，可来自骨架数据 |

### 3.3 UGC 字段（赛题"不踩雷 + 个性化"的核心）

#### `ugcs`（评论列表）

每个 POI **必须有 5-10 条 UGC**，不要少。每条结构：

```json
{
  "nick": "string，仿真用户昵称（不要直接用'用户1'这种）",
  "userface": "",
  "ispithy": false,
  "score": 4.5,
  "star": 5,
  "content": "string，80-300 字真实评论",
  "photos": [],
  "addtime": 1714003200000
}
```

**生成约束（重要，决定 demo 真实性）：**
- `score` 和 `content` 情绪**必须一致**——5 星不能配"服务太差"，2 星不能配"超棒"
- `addtime` 是**毫秒时间戳**，分布在过去 6 个月内（2025-11 到 2026-05），不要全部集中一天
- `ispithy: true` 控制在每店 1-2 条（"优质评论"标记）
- 至少 30% 的 POI 有 1 条**带具体细节的负面/中性评论**（"上菜慢了 30 分钟"、"周末排队 1 小时"）—— Critic Agent 要靠这种内容判断踩雷
- **多人设视角混合**：情侣、家庭带孩子、独行、商务、银发——参考赛题方案文档第 4 章六种 traveler_type
- 文风从 `xhs_深圳*.json` 的小红书笔记里学，**不要 LLM 标准腔**

#### `reviewTags`（评论标签聚合，金矿字段）

每个 POI **必须有 8-15 个 tag**，每个对象：`{tag: "环境优雅", hit: 128}`。

**生成约束：**
- **hit 数差异要大**：最高 hit ~ reviewCount × 0.3，最低 hit 5-15。不要全部 hit=100
- **正负面混合**：8-12 个正面 + 1-3 个负面（"上菜慢"、"价格偏贵"、"位置难找"、"分量小"、"等位久"、"服务一般"）—— **必须有负面 tag**，否则 Critic 没判断依据
- 正面 tag 池：`["环境优雅","服务好","菜品精致","性价比高","氛围佳","干净卫生","食材新鲜","出片漂亮","适合约会","适合聚会","亲子友好","老字号","本地特色","上菜快","包厢私密","交通方便"]`
- 负面 tag 池：`["上菜慢","等位久","价格偏贵","分量小","服务一般","油烟大","厕所老旧","停车难","位置难找","菜量虚标"]`
- **不同 traveler_type 倾向不同 tag**——亲子餐厅"亲子友好" hit 高，约会店"适合约会" hit 高，要内部一致

### 3.4 商场类 POI 的扩展字段

如果该 POI 是商场（categories 含"购物"且名字含"广场/中心/天地/万象"等关键词），加 `mallInfo`：

```json
{
  "popularShops": ["openshopid_xxx", "openshopid_yyy"],
  "dzPopularShops": ["..."],
  "discount": true
}
```

`popularShops` 是该商场内的高人气**美食** POI 的 openshopid 列表（5-10 个），`dzPopularShops` 是**到综**（娱乐/丽人/亲子）类，**这些 ID 必须真实存在于你生成的同城 POI 池里**——这个引用关系是 Planner 做"商场内行程"的入口，不能虚指。

### 3.5 团单 / 外卖辅助字段（可空 list，但建议 30% POI 给）

`dealInfo` 团单优惠：`[{dealName, originPrice, discountPrice, dealPicUrl: "", shopName, type: 1 或 2}]`，type=1 美食，type=2 到综。**discountPrice 必须 < originPrice**，折扣 5-7 折之间合理。

`takeawayinfo`：`{tag: "满 30 减 10", longTag: "...", url: "", mUrl: ""}`，对 `takeawayable: true` 的 POI 选择性给。

### 3.6 不生成的字段（明确忽略）

以下字段在 schema 里有，但本期数据**留空字符串或 null**，避免无意义负担：
- `headPic` / `shopPics`（图片 URL）—— 留 `""` 或 `[]`
- `mShopInfoUrl` / `appShopInfoUrl` 等 6 个跳转链接 —— 留 `""`
- `shopI18ns` 多语言 —— 留 `[]`
- `shopDesc` —— 留 `""`（节省 token，下游不依赖）

---

## 4. 数据规模与分布

### 4.1 数量目标

| 城市 | POI 数量 | 备注 |
|---|---|---|
| 深圳 | 800 ± 50 | 主战场（demo 案例最多）|
| 上海 | 800 ± 50 | |
| 西安 | 800 ± 50 | |

### 4.2 类目分布（每个城市内）

| 类目（一级） | 占比 | 数量参考 |
|---|---|---|
| 美食 | 50% | 400 |
| 休闲娱乐（含景点 / 文化场馆） | 20% | 160 |
| 购物（商场 / 街区） | 10% | 80 |
| 亲子 | 5% | 40 |
| 丽人 / 运动健身 | 5% | 40 |
| 酒店 | 5% | 40 |
| 其他（咖啡 / 酒吧 / 生活服务等） | 5% | 40 |

类目枚举严格用：**美食 / K歌 / 购物 / 电影演出赛事 / 休闲娱乐 / 周边游 / 宴会 / 运动健身 / 丽人 / 结婚 / 酒店 / 爱车 / 亲子 / 学习培训 / 生活服务 / 医疗健康 / 家居 / 宠物 / 榛果民宿 / 交通枢纽**。

### 4.3 商圈分布（地理聚簇要求）

每城识别 **8-12 个核心商圈**作为 anchor，POI 围绕商圈聚簇分布（每个商圈 50-100 个 POI），不要全城均匀撒点——**因为路线规划是按商圈聚簇做的（Anchor & Orbit 模型）**，均匀撒点会让聚类结果烂。

参考商圈（你可以联网搜索补全）：
- **深圳:** 福田 CBD / 华强北 / 海岸城 / 万象天地 / 华侨城 / 蛇口 / 东门老街 / 龙华壹方天地 / 龙岗大运 / 宝安西乡
- **上海:** 陆家嘴 / 南京路步行街 / 新天地 / 田子坊 / 静安寺 / 徐家汇 / 五角场 / 豫园 / 武康路 / 前滩太古里
- **西安:** 大唐不夜城 / 钟楼 / 回民街 / 大雁塔 / 永宁门 / 小寨赛格 / 高新万达 / SKP / 曲江池

---

## 5. 真实性策略（评委一眼判断的东西）

**强 grounding 原则**——这是这份数据有没有"质感"的分水岭：

1. **POI 名字**用 cleaned 数据 / 联网搜索的真实名字。**绝不允许 LLM 编**"深圳市福田区第一火锅店"这种廉价名字。
2. **坐标**用骨架数据，没有的话用商圈中心点 ± 0.005 度随机扰动（约 500 米半径内）。
3. **地址**和坐标 + 行政区匹配——不要"龙岗区华强北路"。
4. **品牌一致性**：海底捞、星巴克、太二、西贝这种连锁品牌，多个分店时 `name` 一致 `branch_name` 不同（"海底捞·万象天地店"）。
5. **价格分布合理**：西安人均餐饮中位数 ~80，上海 ~150，深圳 ~120——不要西安出现一堆人均 800 的店（除非是黑珍珠）。
6. **节假日 / 时令**：可以在部分 UGC 里提到"五一去"、"中秋人多"这类时间锚点，提升真实感（但不要每条都提）。

---

## 6. 工作流程（推荐顺序，可调整）

### Step 1: 探索资产（30 分钟）
- 读 `meituan_hackathon_plan.md` 第 5 章字段契约
- 打开 cleaned 三城 JSON，统计有多少条、哪些字段可复用
- 打开 1-2 个 xhs JSON，感受小红书评论语气

### Step 2: 联网补全 anchor（1-2 小时）
- 三城各搜出 10 个核心商圈名 + 中心坐标
- 搜每城黑珍珠 / 必吃榜清单（用于 `isBlackPearl` grounding）
- 搜热门连锁品牌名单

### Step 3: 写生成器（2-4 小时）
建议结构：

```
mtagent/data_generator/
  generate.py              # 主入口
  schemas.py               # 输出 schema 校验（参考字段契约）
  enrich_with_llm.py       # LLM 补全富字段（ugcs / reviewTags / dishs）
  prompts/
    ugc_prompt.md          # UGC 生成 prompt
    review_tags_prompt.md  # reviewTags 生成 prompt
  load_skeleton.py         # 加载 cleaned + amap 骨架数据
  output/
    shenzhen.json
    shanghai.json
    xian.json
```

#### LLM 选型（两段式策略，必读）

任务特点：~2400 次大调用 + 中文创意写作 + 严格 schema + 速度成本敏感。

**第一段 — 质量基准（仅前 50 条深圳 POI）：用 MiniMax-M2.7。** 这是这批可选模型里中文创意写作天花板，跑出来的 UGC 当作"good example baseline"存档。

**第二段 — 批量主力（剩余全部 ~2350 条）：用 DeepSeek-V4-Flash。** 中文 + 性价比 + 指令遵循三档第一名，速度匹配"一夜跑完"诉求。**先用它跑 50 条同样的深圳 POI，diff M2.7 baseline——如果质量接近（≥ 80% 自然度）就主力 V4-Flash；明显塌方就升级到 Kimi-K2.6（长上下文 + 中文稳）或全程 MiniMax-M2.7（贵但保质）。**

**禁用：** Hy3 preview（preview 版批量任务有翻车风险）/ Auto（黑盒路由不可控）/ GLM-5v-Turbo（视觉版浪费）/ DeepSeek-V3.2 / GLM-5.0-Turbo / MiniMax-M2.5 / Kimi-K2.5（被同代新版替代）。

#### Few-shot 强制要求（决定评论真实度）

**所有 UGC 生成的 prompt 必须包含 3-5 条 `xhs_深圳*.json` 里的真实笔记片段作为 few-shot example**，不允许裸 prompt 直接让模型自由发挥——模型没有锚点会立刻退化到 LLM 标准腔（"这家店环境优雅服务周到"）。

具体做法：
- 启动时从 `xhs_深圳美食_*.json` / `xhs_深圳探店_*.json` / `xhs_深圳景点_*.json` 各抽 1-2 篇 body 字段（300-500 字片段），放进 prompt 的 `<reference_style>` 块
- **按 traveler_type 分组维护 few-shot 池**：情侣店用情侣视角的 xhs 笔记、亲子店用亲子视角、独行店用独行视角——不要混用
- 每生成 50 条 POI 轮换一次 few-shot 样本，避免模型陷入单一文风复读
- reviewTags 和 dishs 字段也要 few-shot：reviewTags 给 2-3 个真实分布参考（hit 数有梯度 + 含负面），dishs 给 2-3 个真实菜单结构参考

**评论自然度的 80% 来自这件事，模型选择只贡献剩下 20%。** 哪怕用 DeepSeek-V4-Flash + 充分 few-shot，质量也大概率超过 MiniMax-M2.7 + 裸 prompt。

### Step 4: 小批量验证（1 小时）
先生成深圳 50 条，**人工 review 10 条**确认质量过线，再批量。**不要直接跑 800 条发现风格不对全废**。

### Step 5: 批量生成（一夜 / 几小时 LLM 跑）
深圳 800 → 上海 800 → 西安 800。**做断点续跑**，避免中途崩了从头来。

### Step 6: 自检 + 交付
跑完后做第 8 节的全部验收检查，过了才交。

---

## 7. 输出格式

### 7.1 文件结构

```
mtagent/data/mock_dianping/
  shenzhen.json       # 800 条 POI list
  shanghai.json
  xian.json
  index.json          # 全局索引：{city: {category: [openshopid, ...], malls: [...]}}
  metadata.json       # {generated_at, total_count, version, source: "llm-mock-v1"}
```

### 7.2 单文件格式

每个城市文件是一个 JSON 数组，**每个元素是一个 POI 对象**（结构按第 3 节字段契约）。

```json
[
  {"openshopid": "...", "name": "...", ...},
  {"openshopid": "...", ...},
  ...
]
```

### 7.3 索引文件结构

`index.json` 用于下游 search 接口仿真层快速召回：

```json
{
  "深圳": {
    "by_category": {"美食": ["openshopid1", ...], "购物": [...], ...},
    "by_district": {"福田区": [...], "南山区": [...], ...},
    "by_mall": {"万象天地": [...], "海岸城": [...], ...},
    "by_keyword": {"火锅": [...], "粤菜": [...], ...}
  },
  ...
}
```

---

## 8. 验收标准（交付前自检）

写一个 `validate.py` 跑下面这些断言，全过才算交付：

### 8.1 Schema 完整性
- [ ] 每个 POI 第 3.1 节必填字段全部存在
- [ ] `openshopid` 全局唯一
- [ ] 类型正确（int / float / string / list / bool 不混淆）
- [ ] `categories` 一级类目在 20 枚举内

### 8.2 数量与分布
- [ ] 三城各 800 ± 50
- [ ] 类目分布偏差 ≤ 5%
- [ ] 每个商圈 anchor 至少 30 个 POI

### 8.3 内部一致性
- [ ] `star` 高的 POI `reviewCount` 也高（相关系数 > 0.3）
- [ ] 餐饮类 100% 有 `dishs`（≥ 3 道）
- [ ] `mallInfo.popularShops` 引用的 ID **全部存在于同城 POI 池**
- [ ] `business_hour` 全部匹配格式 `\d{2}:\d{2}-\d{2}:\d{2}`
- [ ] `score` 和 `star` 关系正确（star = round(score)）

### 8.4 真实性抽样（人工 / LLM judge）
随机抽 30 条（每城 10 条）：
- [ ] POI 名字看起来像真实存在的店（不是 LLM 编的廉价名）
- [ ] 评论里**至少能看到 1 条带具体细节的差评**（不全是 "环境好服务好"）
- [ ] 评论文风**不像 LLM 标准腔**（参考小红书原文）
- [ ] 地址和 district 在地理上匹配
- [ ] reviewTags 有正有负 hit 数有差异

### 8.5 可被下游消费
- [ ] 用 Pydantic 解析 100% 通过（schema 文件由下游 Agent 工程提供，本期未必有，可以暂留 placeholder）

---

## 9. 反模式（不要做）

- ❌ **LLM 标准腔评论**："这家店环境优雅，服务周到，菜品精致，强烈推荐！"——这种话生成 1000 条都没用，评委一秒识别。**学小红书笔记的口语化、emoji、断句**。
- ❌ **POI 名字编造**："美味火锅店"、"幸福餐厅"——必须用真实存在的名字。
- ❌ **坐标乱撒**：上海的 POI 出现在深圳坐标范围内。
- ❌ **类目名乱写**：用"小吃"、"火锅店"——必须用 20 个一级枚举。
- ❌ **reviewTags 全正面**：每店至少 1 个负面 tag，否则 Critic 无内容可判。
- ❌ **`isBlackPearl=1` 滥用**：每城上限 30 个，必须人均 ≥ 500 高端餐饮。
- ❌ **`addtime` 全部今天**：分布在过去 6 个月。
- ❌ **`mallInfo.popularShops` 引用不存在的 ID**：必须是同城真实生成的 POI。
- ❌ **一个生成器 prompt 跑 2400 条**：分批 + 多样化人设，避免雷同。

---

## 10. 交付清单

完成时给出：

1. **`mtagent/data/mock_dianping/` 目录**及其下 5 个文件（3 城 JSON + index + metadata）
2. **`mtagent/data_generator/` 工具代码**（生成器源码，让人能复跑 / 增量补数据）
3. **一份 200 字内的交付说明**：
   - 总条数 / 类目分布 / 黑珍珠数量 / 商圈分布
   - 用了哪个 LLM、调用次数、大概成本
   - 哪些验收项没过 + 原因（如果有）
   - 已知的 100 条样本能 demo 哪些场景（"情侣 3 天深圳" 等）

---

## 11. 不确定时的判断原则

- 字段含义不清：**忠实点评接口文档原文**，不要自创
- 数据量不够：**质量优先于数量**，宁可 600 条精的不要 800 条糙的
- 时间不够：**深圳必须满，上海西安可以减半**（深圳是主 demo 城市）
- 与本文档冲突：**优先和 Banz 沟通**，不要自己拍板改契约

---

> **最后一句话:** 这份数据是整个项目的真实数据基础，不是临时 mock。POI 名字真实 + 评论像人说话 + reviewTags 有正有负 hit 数有梯度——这三件事做到位，下游 Agent 工程就能讲出真实的故事。糊弄一条都会在 demo 时被评委一眼识破。
